# C2 progress (paused 2026-09-25)

Plan: `artifacts/2026-09-25-office-implementation-plan.md` C2. Nothing committed. Code for all five
items is written; the generated contract, verification, formatting and docs remain.

## Done (code written)

1. **Trimmed effects.** `collaboration/src/sourceDocuments.ts`: `trimEffect` (changed span + 40 chars
   each side, `…` marks a cut, never splits a surrogate pair) applied in `effects()` after
   `compareBaselines` and to `rebaseOffice` effects in `rebasePublication` (netTokens from the trimmed
   list). Text-format effects untouched (already exact spans). `pipeline/pipeline/retrieval/pending.py`
   message now says before/after are excerpts and drops "descriptions or summaries".
2. **Trigger.** TS constants `OFFICE_REFRESH_TOKENS/IDLE/STALE` passed as `$1..$3` to the
   `scheduleRefreshes` query; Go constants `officeRefreshTokens/Idle/Stale` in
   `server/internal/store/source_documents.go` (due = tokens >= 3000 or unedited >= 7 days, always
   after 60 s idle). refresh_error handling unchanged.
3. **Descriptor-only + 2% gate.** `prompts/ingest.py` (descriptor-only JSON, `SUMMARY_VERSION = 2`,
   `summary_messages(body)`); `retrieval/indexing.py` (`_descriptor` gate, `SUMMARY_REUSE_SHARE = 0.02`,
   `text_change_tokens` ported from `probes/delta/delta.py` `text_changes`, `summarize_file` returns
   one string, `_PARTIAL_WORDS = 500` replaces the size tiers for map-reduce partials);
   `retrieval/store.py` (`published_summary`, `upsert_content_summary(change_share, summary_version)`,
   donor copy, `workspace_outline` without summary, `file_summaries` deleted); `retrieval/tools.py`
   (describe_documents, `_describe_note`, `_get_json`, `required_file_ids` deleted); `retrieval/agent.py`;
   `prompts/chat.py` line; Go contract removed in `agenttools/agenttools.go`; `store/share.go` clone;
   `ops/privileges.go`; migration `server/migrations/0029_descriptor_summaries_platform_payer.sql`
   (drops `summary`, adds `change_share`, adds `provider_sessions.platform_paid`); lab playground
   tool lists (`lab/playground/scripts/playground.py`, `configs/chat.json`, `README.md`, `scripts/ui.html`).
4. **Parse fee.** Go refresh payload `parseFee: !ever && !platformPaid`; `worker.py` `_parse_fee(payload)`
   and `_record_parse_usage_tx(charged=...)` (credit 0, pages still recorded, metadata `parseFee`).
   Admission already reserves 0 and keeps the exhaustion check, so no Go admission change was needed.
5. **Platform payer.** Unexported `requestSourceRefresh(..., platformPaid)` (C4 will call it);
   `beginPlatformIngestSessionTx` in `store/credits.go` (no credit check, no lease); platform sessions
   excluded from both ingest-lease counts; payload `platformPaid`; `worker.py` skips the claim-time
   credit check for it; `db.settle_ingest_provider_call` zeroes credits and writes
   `metadata.platformPaid=true` for a `platform_paid` session.

Tests written: `collaboration/src/sourceDocuments.test.ts` (trim); Go `TestOfficeAutomaticRefreshAdmission`,
`TestSourceRefreshParseFeeAndPlatformPayer`; pipeline `test_indexing.py` (gate integration, first
descriptor), `test_retrieval_helpers.py` (2% boundary, version change, no previous; reflow diff),
`test_ingest_worker.py` (parse fee), `test_store_sql.py` (platform settlement); describe_documents
tests removed or switched to list_sources/read_document in `test_agent.py`, `test_evidence.py`.

## Next steps, in order

1. `pnpm gen:openapi` to regenerate `pipeline/pipeline/generated/agent_tools.json` (still lists
   describe_documents); check the rest of the generated diff is only that.
2. Run `pnpm test:pipeline`, `pnpm test:go`, `pnpm test:collaboration`, `pnpm typecheck`; fix failures.
   Known unrelated: Go `copytext` (Windows blocks the binary), store `TestColumnLimits` for
   `pdf_annotations.text`.
3. `pnpm run fmt`, `pnpm run fix`, `pnpm run fmt:go`, `pnpm run fmt:py`.
4. Docs: `openwiki/agentic-retrieval.md` (lines near 95, 122, 1160-1166, 1207-1212, 2074, 2335,
   2391-2392, 2442-2444, 2476-2477; keep the existing uncommitted lines near 1889-1894),
   `openwiki/observability-metering.md` (ingest billing near 637-735: refresh parse uncharged,
   platform-paid sessions), `openwiki/test-catalog.md` (new/renamed/removed tests above).
5. `human/`: only add code references to agentic-retrieval.md records at lines 26 and 34 and
   observability-metering.md line 28.

## Tests run so far

- `npx vitest run --dir collaboration collaboration/src/sourceDocuments.test.ts`: 11 passed.
- `go build ./...` and `go vet` on store, agenttools, ops: clean. Nothing else run yet.

## Open questions for the developer

- First parse through a refresh (store-only file edited, then processed manually): charged per the
  record ("fee only on a file's first parse"); the plan line says every refresh parse is uncharged.
  Recommend keeping the charge, else store-only upload plus one edit parses for free.
- "Recorded against the platform": usage rows keep the file owner as actor (FK is NOT NULL) at 0
  credits with `metadata.platformPaid`; the session carries `platform_paid`. Recommend accepting.
- Platform sessions do not count toward the owner's 20 ingest leases. Recommend accepting.
- A `move` effect (same text) trims to its last 40 characters, as the measured reference probe did.
- The running share is stored as a sum of per-refresh shares, not cumulative tokens over the
  current size. Differences are second order.
- `human/agentic-retrieval.md` records at lines 134 and 226 still name describe_documents (superseded
  by line 34); only the developer can edit them.
- `human/frontend/office-files.md` shows as modified in the tree; not changed by this run.
