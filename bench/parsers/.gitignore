# Benchmark inputs and local outputs are artifacts, not source.
# fixtures/docs/ holds real uploads (possibly someone's lecture notes) — never commit it.
fixtures/docs/
reports/local/
.DS_Store
*.failure.log
