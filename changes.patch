
diff --git a/messages/en.json b/messages/en.json
index 8f92c0a..d46ed07 100644
--- a/messages/en.json
+++ b/messages/en.json
@@ -263,7 +263,7 @@
   "cloud_connect_dismiss": "Not now",
   "cloud_connect_action": "Connect",
   "error_generic_title": "Something went wrong",
-  "error_generic_body": "Please try again. If the problem continues, refresh the page.",
+  "error_generic_body": "Please retry or refresh the page. If the problem continues, please contact support for help.",
   "error_network_title": "Connection problem",
   "error_network_body": "We couldn't reach Capy Notebook. Check your connection and try again.",
   "error_offline_title": "You're offline",
@@ -301,7 +301,8 @@
   "error_file_empty_body": "The file is empty or corrupted. Upload it again and retry.",
   "error_not_found_page_title": "Page not found",
   "error_not_found_page_body": "The page may have moved or the address may be incorrect.",
-  "error_action_retry": "Try again",
+  "error_action_retry": "Retry",
+  "error_action_funny": "Rewind Time",
   "error_action_reload": "Reload",
   "error_action_go_back": "Go back",
   "connection_waiting": "Waiting for connection",
diff --git a/messages/zh.json b/messages/zh.json
index 269c38d..2ce5b21 100644
--- a/messages/zh.json
+++ b/messages/zh.json
@@ -299,6 +299,7 @@
   "error_not_found_page_title": "找不到页面",
   "error_not_found_page_body": "页面可能已移动，或地址不正确。",
   "error_action_retry": "重试",
+  "error_action_funny": "时间回溯",
   "error_action_reload": "重新加载",
   "error_action_go_back": "返回",
   "connection_waiting": "等待网络连接",
diff --git a/src/components/app/AppErrorBoundary.tsx b/src/components/app/AppErrorBoundary.tsx
index b345509..7c8cb5d 100644
--- a/src/components/app/AppErrorBoundary.tsx
+++ b/src/components/app/AppErrorBoundary.tsx
@@ -1,16 +1,16 @@
-import { useQueryErrorResetBoundary } from '@tanstack/react-query';
-import { type ErrorComponentProps, Link } from '@tanstack/react-router';
-import { Component, type ErrorInfo, type ReactNode } from 'react';
-import { Button } from '@/components/ui/Button';
-import { m } from '@/i18n';
-import { describeError, privateErrorDescription } from '@/lib/errors';
-import { ErrorState } from './ErrorState';
-import { Panel } from './layout';
+import { useQueryErrorResetBoundary } from "@tanstack/react-query";
+import { type ErrorComponentProps, Link } from "@tanstack/react-router";
+import { Component, type ErrorInfo, type ReactNode } from "react";
+import { Button } from "@/components/ui/Button";
+import { m } from "@/i18n";
+import { describeError, privateErrorDescription } from "@/lib/errors";
+import { ErrorState } from "./ErrorState";
+import { Panel } from "./layout";
 
 interface BoundaryProps {
   children: ReactNode;
   resetKeys?: readonly unknown[];
-  variant?: 'page' | 'panel';
+  variant?: "page" | "panel";
 }
 
 interface BoundaryCoreProps extends BoundaryProps {
@@ -23,7 +23,7 @@ interface BoundaryState {
 
 function resetKeysChanged(
   previous: readonly unknown[] | undefined,
-  next: readonly unknown[] | undefined
+  next: readonly unknown[] | undefined,
 ): boolean {
   if (previous === next) return false;
   if (!previous || !next || previous.length !== next.length) return true;
@@ -38,7 +38,7 @@ class BoundaryCore extends Component<BoundaryCoreProps, BoundaryState> {
   }
 
   componentDidCatch(error: unknown, info: ErrorInfo) {
-    console.error('Unhandled React error', error, info.componentStack);
+    console.error("Unhandled React error", error, info.componentStack);
   }
 
   componentDidUpdate(previous: BoundaryCoreProps) {
@@ -59,7 +59,7 @@ class BoundaryCore extends Component<BoundaryCoreProps, BoundaryState> {
     if (!this.state.error) return this.props.children;
 
     const description = describeError(this.state.error);
-    const reload = description.action === 'reload';
+    const reload = description.action === "reload";
     return (
       <main className="h-svh overflow-hidden p-1.5 sm:p-2.5">
         <Panel
@@ -110,16 +110,23 @@ export function RouteErrorComponent({ error, reset }: ErrorComponentProps) {
   };
 
   return (
-    <ErrorState
-      action={
-        <Button onClick={reload ? () => window.location.reload() : retry}>
-          {reload ? m.error_action_reload() : m.error_action_retry()}
-        </Button>
-      }
-      description={description.description}
-      title={description.title}
-      variant="page"
-    />
+    <main className="h-full overflow-hidden p-1.5 sm:p-2.5">
+      <Panel
+        className="h-full w-full"
+        sectionClassName="h-full w-full min-h-full flex flex-row"
+      >
+        <ErrorState
+          action={
+            <Button onClick={reload ? () => window.location.reload() : retry}>
+              {reload ? m.error_action_reload() : m.error_action_retry()}
+            </Button>
+          }
+          description={description.description}
+          title={description.title}
+          variant="page"
+        />
+      </Panel>
+    </main>
   );
 }
 
@@ -128,36 +135,50 @@ export function ShareRouteErrorComponent({ reset }: ErrorComponentProps) {
   const description = privateErrorDescription();
 
   return (
-    <ErrorState
-      action={
-        <Button
-          onClick={() => {
-            resetQueries();
-            reset();
-          }}
-        >
-          {m.error_action_retry()}
-        </Button>
-      }
-      description={description.description}
-      testId="private-or-unavailable"
-      title={description.title}
-      variant="page"
-    />
+    <main className="h-full overflow-hidden p-1.5 sm:p-2.5">
+      <Panel
+        className="h-full w-full"
+        sectionClassName="h-full w-full min-h-full flex flex-row"
+      >
+        <ErrorState
+          action={
+            <Button
+              onClick={() => {
+                resetQueries();
+                reset();
+              }}
+            >
+              {m.error_action_funny()}
+            </Button>
+          }
+          description={description.description}
+          testId="private-or-unavailable"
+          title={description.title}
+          variant="page"
+        />
+      </Panel>
+    </main>
   );
 }
 
 export function RouteNotFoundComponent() {
   return (
-    <ErrorState
-      action={
-        <Button asChild iconLeft="chevronLeft">
-          <Link to="/">{m.error_action_go_back()}</Link>
-        </Button>
-      }
-      description={m.error_not_found_page_body()}
-      title={m.error_not_found_page_title()}
-      variant="page"
-    />
+    <main className="h-full overflow-hidden p-1.5 sm:p-2.5">
+      <Panel
+        className="h-full w-full"
+        sectionClassName="h-full w-full min-h-full flex flex-row"
+      >
+        <ErrorState
+          action={
+            <Button asChild iconLeft="chevronLeft">
+              <Link to="/">{m.error_action_go_back()}</Link>
+            </Button>
+          }
+          description={m.error_not_found_page_body()}
+          title={m.error_not_found_page_title()}
+          variant="page"
+        />
+      </Panel>
+    </main>
   );
 }
diff --git a/src/components/ui/Button.tsx b/src/components/ui/Button.tsx
index 897d234..55cab62 100644
--- a/src/components/ui/Button.tsx
+++ b/src/components/ui/Button.tsx
@@ -89,7 +89,9 @@ export function Button({
         data-variant={variant}
         {...rest}
       >
+        {iconLeft && <InlineIcon name={iconLeft} size={size} />}
         {children}
+        {iconRight && <InlineIcon name={iconRight} size={size} />}
       </Slot.Root>
     );
   }
diff --git a/src/components/ui/Menu.tsx b/src/components/ui/Menu.tsx
index 61fc789..d3429ad 100644
--- a/src/components/ui/Menu.tsx
+++ b/src/components/ui/Menu.tsx
@@ -103,7 +103,7 @@ export function Menu({
             onSelect={() => it.onClick?.()}
           >
             {it.icon && <Icon className="-translate-y-px" name={it.icon} />}
-            <span className="translate-y-px">{it.label}</span>
+            <span>{it.label}</span>
           </DropdownMenuItem>
         ))}
       </DropdownMenuContent>

          
      