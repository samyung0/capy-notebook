# Production env for deploy/docker-compose.prod.yml.
# Copy to deploy/.env.prod for a manual compose up, or paste into Coolify
# Environment Variables (mark passwords/keys as secrets).
#
# Generate secrets with: openssl rand -hex 32
# Postgres password must not contain @ : / ? # or the DATABASE_URL breaks.

# ── Required on first deploy ────────────────────────────────────────────────
POSTGRES_PASSWORD=
APP_URL=https://abcd.com
CORS_ALLOWED_ORIGINS=https://abcd.com,https://www.abcd.com
COLLABORATION_URL=wss://collab.abcd.com
COLLABORATION_ALLOWED_ORIGINS=https://abcd.com
COLLABORATION_SECRET=
PIPELINE_SECRET=
LLM_CREDENTIALS_KEY=
CLERK_SECRET_KEY=
CLERK_WEBHOOK_SECRET=

B2_ENDPOINT=https://s3.us-west-004.backblazeb2.com
B2_REGION=
B2_BUCKET=
B2_KEY_ID=
B2_APP_KEY=

# ── Provider keys (elitellm platformEnv names for hops you actually run) ──
# Anthropic platform traffic uses DeepInfra.
DEEPSEEK_API_KEY=
OPENAI_API_KEY=
DEEPINFRA_API_KEY=
OPENROUTER_API_KEY=
GEMINI_API_KEY=
EMBEDDING_DIM=2560
EVO_QUERY_MODEL=deepseek-v4-flash-vision-exp

# ── Mail / billing ──────────────────────────────────────────────────────────
EMAIL_BACKEND=resend
RESEND_API_KEY=
EMAIL_FROM=Evo Notes <notifications@example.com>
EMAIL_REPLY_TO=
EMAIL_UNSUBSCRIBE_SECRET=
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_PRO=
STRIPE_PRICE_TEAM=

# ── Private parser network ──────────────────────────────────────────────────
# Address on the app host's WireGuard interface. Postgres and Redis bind here.
# Leave unset before WireGuard exists; Compose then binds them to loopback.
EVO_PRIVATE_BIND_ADDRESS=10.77.0.1
# Native PostgreSQL already owns port 5432 on the Coolify host. This affects
# only the private host publish; containers still connect to db:5432.
EVO_PRIVATE_POSTGRES_PORT=55432

# The production ingest worker runs from docker-compose.parser-vm.yml. These
# legacy values are used only if COMPOSE_PROFILES=legacy-modal-worker is set for
# rollback testing on this branch.
MODAL_FAST_PARSE_URL=
MODAL_PARSE_TOKEN=
EVO_PARSE_METHOD=selective_rapidocr

# ── Observability ───────────────────────────────────────────────────────────
SENTRY_DSN_GATEWAY=
SENTRY_DSN_RETRIEVAL=
SENTRY_DSN_WORKER=
SENTRY_DSN_COLLABORATION=
SENTRY_DSN_OPS=
VITE_SENTRY_DSN_OPS=
SENTRY_TRACES_SAMPLE_RATE=0.1
# Coolify Git deploys inject SOURCE_COMMIT; set this only for a CLI deploy.
RELEASE_SHA=

# ── Import relay (optional) ─────────────────────────────────────────────────
IMPORT_RELAY_ENQUEUE_URL=
IMPORT_RELAY_SECRET=

# ── After §8 grants: add these, then set COMPOSE_PROFILES=ops ───────────────
# Host is `db` while Postgres is this compose service.
# OPS_DATABASE_URL=postgres://evo_ops:<password>@db:5432/evo?sslmode=disable
# OPS_AUTH_DATABASE_URL=postgres://evo_ops_auth:<password>@db:5432/evo?sslmode=disable
# OPS_REGISTRY_DATABASE_URL=postgres://evo_ops_registry:<password>@db:5432/evo?sslmode=disable
# OPS_ACTIONS_DATABASE_URL=postgres://evo_ops_actions:<password>@db:5432/evo?sslmode=disable
# OPS_CF_ACCESS_ISSUER=https://<team>.cloudflareaccess.com
# OPS_CF_ACCESS_AUDIENCE=
# VITE_CLERK_PUBLISHABLE_KEY=
# COMPOSE_PROFILES=ops

# ── Optional ────────────────────────────────────────────────────────────────
# WORKER_REPLICAS=1
# RATE_LIMIT_AI_PER_HOUR=200
