- Developer Epo decided uploaded-audio ingest uses synchronous ElevenLabs Scribe v2 without webhook, polling, provider-transcript, or external-wait state; the ingest attempt checks the source-hash cache, opens accounting, submits the source URL, validates the response, settles the charge, caches the transcript, and continues indexing while retaining duration billing, the 10-hour input cap, source-hash locking, and weighted provider admission. pipeline/pipeline/ingest/source_text.py pipeline/pipeline/ingest/worker.py pipeline/pipeline/config.py pipeline/pipeline/jobs.py server/migrations/0001_init.sql
- Developer Epo decided optional parse-bundle, derived-text, and caption artifact writes get exactly three total storage attempts and remain best effort; required source downloads, local parser handoff, and Office preview publication remain strict. pipeline/pipeline/store/blobstore.py pipeline/pipeline/ingest/worker.py pipeline/pipeline/parse/figures.py
- Developer Epo decided search returns the hit chunk only; ±1 neighbour expansion is gone because a 1600-character packed chunk plus 200-character block overlap is the context, and read_document covers a packing cut. pipeline/pipeline/retrieval/search.py pipeline/pipeline/retrieval/store.py
- Developer Epo decided EVO_SEARCH_TOP_K defaults to 5. pipeline/pipeline/config.py
- Developer Epo decided chat does not search before the first model call; the agent issues at most one search_workspace per model response and may search again on a later response in the same turn. pipeline/pipeline/retrieval/agent.py pipeline/pipeline/retrieval/limits.py pipeline/pipeline/retrieval/tools.py
- Developer Epo decided the lexical index and query use the Postgres `english` text-search configuration (stemming, English stopwords; CJK bigrams pass through), lexical ranks weigh 0.5 of vector ranks in RRF, and EVO_SEARCH_PER_FILE_CAP defaults to 4; measured on the ingest-VM lab corpus, hits@5 went 19 -> 26 of 28 with these three changes together. pipeline/pipeline/retrieval/store.py pipeline/pipeline/config.py pipeline/pipeline/retrieval/chunking.py
- Developer Epo decided chunks pack by estimated tokens (EVO_CHUNK_TOKENS 400, overlap 50, min 40) instead of characters, so CJK chunks carry the same token load as Latin ones. pipeline/pipeline/retrieval/chunking.py pipeline/pipeline/config.py
- Developer Epo decided parser table HTML is flattened to pipe-separated rows, `<sub>`/`<sup>` tags are stripped with their content kept, and spacing inside inline LaTeX is collapsed; formulas are otherwise left as written. pipeline/pipeline/retrieval/chunking.py
- Developer Epo decided lexical candidates that match every query term rank ahead of partial matches before ts_rank_cd ordering, so identifiers like "Figure 3.20" beat passages that merely repeat a frequent term. pipeline/pipeline/retrieval/store.py
- Developer Epo decided (2026-09-04, overriding the two earlier concept decisions) that concept extraction, the `rag_concepts`/`rag_concept_mentions` tables and the search-result concept footer are removed: the footer was navigational only (never a ranking signal), on the lab corpus 910 of 1,663 concepts were named in a single chunk and could only point back at the passage already shown, and no chat trace showed the model following a footer name into another document; multi-hop stays with the agent loop (read the hits, search again for the names found there) over hybrid search. Measured before removal on the lab corpus with 13 two-document questions (`pipeline/scripts/rag_eval/questions-*-bridge.json`, es/fr/ja/zh), two passes per build: with the footer 24 of 26 turns returned and cited an expected passage, without it 23 of 26, and every turn on both builds produced the cross-document answer (the strict misses answered from duplicate or adjacent passages); follow-up searches took their new terms from the hit text on both builds, so the hop came from the agent loop rather than the footer. Reopen only if `rag_search_events` shows a measured bridging failure: a run of the eval sets with deliberately two-document questions where the cited passage is absent from every search in the turn, or a turn pattern of three or more searches whose hits overlap and whose answer cites nothing; if that happens the LinearRAG shape (cheap NER/keyphrase extraction, one-hop co-mention expansion as a third RRF leg) is the candidate, not LLM extraction. pipeline/pipeline/retrieval/indexing.py pipeline/pipeline/retrieval/store.py pipeline/pipeline/retrieval/tools.py server/migrations/0001_init.sql server/internal/store/share.go
- Developer Epo decided a lexical candidate that matches every term of a two- or three-term query counts at full weight in RRF (longer all-terms matches and partial matches stay at 0.5), so identifier lookups like "Figure 3.20" can enter the top five when the embedding misses them; measured on the lab identifier set, answerable questions went 13 -> 14 of 14 with the semantic set unchanged. pipeline/pipeline/retrieval/store.py
- Developer Epo decided `tokenize_for_search` carries non-CJK text as whole segments; the previous per-character emission on mixed-script chunks was a bug that erased Latin words from the lexical index whenever OCR produced a stray CJK character. pipeline/pipeline/retrieval/chunking.py
- Developer Epo decided the lexical index supports en, fr, de, es, zh, ja, ko: each chunk carries a detected `lang` (zh-CN/TW/HK are one language), its tsvector is built with that language's Postgres configuration (`simple` for CJK), and the query is parsed once per configuration and matched against chunks of that language; anything undetected is `und`/`simple`. pipeline/pipeline/retrieval/lang.py pipeline/pipeline/retrieval/store.py pipeline/pipeline/retrieval/indexing.py server/migrations/0001_init.sql
- Developer Epo decided the exact tier counts query terms once as typed, independent of language and before any stopword list, with each CJK run counting once regardless of its bigrams, and a lookup additionally carries no function word of the chunk's language (the language configuration leaves as many lexemes as `simple`); this replaced counting lexemes per language after stopwords, which let "What is CamemBERT trained on?" shrink to two terms under `english` and promote bibliography rows on a French corpus, and it means a hyphenated identifier typed as one word ("CIL-LLM") is one term, not a lookup. pipeline/pipeline/retrieval/store.py pipeline/pipeline/retrieval/chunking.py
- Developer Epo decided a search whose hits are at least half repeats of passages already shown in the turn tells the model so in the result footer and asks it to report the gap instead of rewording again. pipeline/pipeline/retrieval/tools.py
- Developer Epo decided RAG search telemetry is one `rag_search_events` row per `search_workspace` call, written at turn end with which hits the answer cited, storing features and ids only (no query or passage text), pruned after 90 days; there is no user thumbs-up/down signal. server/migrations/0001_init.sql pipeline/pipeline/retrieval/store.py pipeline/pipeline/retrieval/agent.py
- Developer Epo decided the lab sample documents stay on the ingest host under /opt/evo-rag-lab/samples/<lang>/ and the retrieval eval harness and its question sets live in the repo under pipeline/scripts/rag_eval. pipeline/scripts/rag_eval.py
- Developer Epo decided a non-heading text block whose text recurs on three or more pages is running page furniture and every copy is dropped at chunking whatever block label the parser gave it (the title itself is a heading and survives in the section path); measured on the lab corpus a paper's title-and-authors line had opened 28 of its 75 chunks, and dropping it took es identifiers 10 -> 13 of 14 and ja natural 11 -> 13 of 14. pipeline/pipeline/retrieval/chunking.py
- Developer Epo decided a numeric superscript glued to a word of three or more letters or to a CJK run is an affiliation or footnote marker and is dropped at chunking, while one- or two-letter units and variables keep their exponent; kept, "Mayor-Rocher1" indexed as `rocher1` and matched no query. pipeline/pipeline/retrieval/chunking.py
- Developer Epo decided a reference list (a `list` block whose items are at least 80% citation-shaped, five or more) is chunked, embedded and readable like any other passage but carries an empty lexical vector, including after a donor copy is re-embedded, so citation titles stop outranking the body that answers a question asked in the bibliography's language; measured on the lab corpus, English questions on Chinese-only content went 5 -> 7 of 9 with every other set unchanged. pipeline/pipeline/retrieval/chunking.py pipeline/pipeline/retrieval/indexing.py pipeline/pipeline/retrieval/store.py
- Developer Epo decided hyphenated identifiers count as one term as typed ("CIL-LLM" is one term and not a two-term lookup); splitting on hyphens was offered and declined. pipeline/pipeline/retrieval/chunking.py
- Developer Epo decided `rag_search_events` records the language of the hits (`hits_lang`, the majority chunk language of the result) rather than a detected query language, because 30 of 42 short French, German and Spanish questions detected as `und` exactly where language support was added; on the lab sets 2 of 268 searches recorded `und`. server/migrations/0001_init.sql pipeline/pipeline/retrieval/search.py pipeline/pipeline/retrieval/store.py
- Developer Epo decided (2026-09-05) Google Drive and OneDrive imports run as an `import` job type in the pipeline's Postgres `jobs` queue on the ingest host; the Cloudflare Worker relay, its Queues, the HMAC relay auth, the gateway dispatcher and the queue outbox columns are removed because Postgres already holds the durable lease/retry state, provider quotas key on OAuth client and user rather than source IP, and the Worker cost a paid plan plus ~1,800 lines for that. pipeline/pipeline/ingest/import_stage.py pipeline/pipeline/ingest/import_worker.py pipeline/pipeline/jobs.py server/internal/httpapi/internal_import.go server/internal/store/source_imports.go server/migrations/0001_init.sql
- Developer Epo decided (2026-09-05) the provider-to-B2 transfer is its own stage that must succeed before any parse or ingest job exists: import work takes its own capacity slot so ingest slots stay free, a retryable transfer error retries against the import attempt budget, and a terminal error or an exhausted budget fails the import and releases its reservation. pipeline/pipeline/ingest/import_stage.py pipeline/pipeline/ingest/worker.py pipeline/pipeline/ingest/capacity.py
- Developer Epo decided (2026-09-05) the Microsoft download host allowlist includes `microsoftpersonalcontent.com`, the allowlist is configuration (`EVO_IMPORT_DOWNLOAD_HOSTS`) rather than a code literal, and every download resolves the host, rejects private/reserved addresses and connects to the validated address with the hostname as SNI. pipeline/pipeline/ingest/pinned_http.py pipeline/pipeline/config.py
- Developer Epo decided (2026-09-05) a provider 401 or 403 on the import download is terminal (the user re-picks the file), a gateway `too_many_ingest_leases` 429 at completion returns the import claim without spending an attempt, a Google bearer grant is sent only to `www.googleapis.com`, `EVO_IMPORT_JOB_TIMEOUT` must stay under the gateway's twelve-minute attempt lease, and import upload sessions expire after one hour so a reaped worker frees the reservation within the hour. pipeline/pipeline/ingest/import_stage.py pipeline/pipeline/config.py server/internal/httpapi/huma_sources.go server/internal/httpapi/internal_import.go
- Developer Epo decided (2026-09-05) a capacity wait at import completion (`too_many_ingest_leases`) releases the gateway attempt lease and backs the queue row off by the gateway's `Retry-After` so the wait costs a few polls instead of a twelve-minute stall, and one import request carries at most 20 files, matching the per-actor ingest lease cap. pipeline/pipeline/ingest/import_stage.py server/internal/httpapi/apimodel/requests.go
- Developer Epo decided (2026-09-05) the gateway answers the held-lease import 409 with the remaining lease seconds as `Retry-After` so the worker waits with the same backoff plumbing as the capacity wait, and a database error while yielding an import claim surfaces as a retryable error instead of closing the queue row. server/internal/httpapi/internal_import.go pipeline/pipeline/ingest/import_stage.py
- The repository developer authorized (2026-09-05) a local small-model quiz-grading comparison using the complete supplied rubric/reference answer, eight subject groups and en/es/fr/ja/ko/zh-Hans/zh-Hant, targeting 50 questions per subject/language group with five answers each (2,800 localized question versions; 14,000 grading cases), reporting Chinese scripts separately, using Codex to author and judge with AI-review provenance, and running a timed pilot before the full comparison. scripts/grading_benchmark/
