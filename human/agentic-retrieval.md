- Developer Epo decided uploaded audio is transcribed synchronously by ElevenLabs Scribe v2 inside the ingest attempt (source-hash cache and lock, weighted provider admission, duration billing, 10-hour input cap) with no webhook, polling or external-wait state. pipeline/pipeline/ingest/source_text.py pipeline/pipeline/ingest/worker.py pipeline/pipeline/config.py
- Developer Epo decided optional parse-bundle, derived-text and caption artifact writes get three storage attempts and stay best effort; source downloads, local parser handoff and Office preview publication stay strict. pipeline/pipeline/store/blobstore.py pipeline/pipeline/ingest/worker.py pipeline/pipeline/parse/figures.py
- Developer Epo decided search returns the hit chunk only, without neighbour expansion; read_document covers a packing cut. pipeline/pipeline/retrieval/search.py
- Developer Epo decided CAPY_SEARCH_TOP_K defaults to 5 and CAPY_SEARCH_PER_FILE_CAP to 4. pipeline/pipeline/config.py
- Developer Epo decided chat makes no search before the first model call; the agent issues at most one search_workspace per model response and may search again on a later response of the same turn. pipeline/pipeline/retrieval/agent.py pipeline/pipeline/retrieval/limits.py
- Developer Epo decided chunks pack by estimated tokens (CAPY_CHUNK_TOKENS 400, overlap 50, min 40) so CJK and Latin chunks carry the same token load. pipeline/pipeline/retrieval/chunking.py pipeline/pipeline/config.py
- Developer Epo decided chunking flattens parser tables to pipe-separated rows, strips `<sub>`/`<sup>` tags keeping their content, collapses spacing inside inline LaTeX and otherwise leaves formulas as written. pipeline/pipeline/retrieval/chunking.py
- Developer Epo decided chunking drops every copy of a non-heading text block that recurs on three or more pages as page furniture (headings survive in the section path), and drops a numeric superscript glued to a word of three or more letters or to a CJK run as an affiliation/footnote marker while one- or two-letter units and variables keep their exponent. pipeline/pipeline/retrieval/chunking.py
- Developer Epo decided a reference list (a `list` block of five or more items, at least 80% citation-shaped) is chunked, embedded and readable like any passage but carries an empty lexical vector, including after a donor copy is re-embedded. pipeline/pipeline/retrieval/chunking.py pipeline/pipeline/retrieval/indexing.py pipeline/pipeline/retrieval/store.py
- Developer Epo decided the lexical index supports en, fr, de, es, zh, ja, ko: each chunk carries a detected `lang` (zh variants are one language, undetected is `und`), its tsvector uses that language's Postgres configuration (`simple` for CJK and `und`), and the query is parsed once per configuration present and matched against chunks of that language; `tokenize_for_search` carries non-CJK text as whole segments and CJK as bigrams. pipeline/pipeline/retrieval/lang.py pipeline/pipeline/retrieval/chunking.py pipeline/pipeline/retrieval/store.py pipeline/pipeline/retrieval/indexing.py
- Developer Epo decided hybrid ranking is RRF with lexical ranks at 0.5 of vector ranks; lexical candidates matching every query term rank ahead of partial matches before ts_rank_cd; a candidate matching every term of a lookup counts at full weight, where a lookup has two or three terms counted once as typed before any stopword list (each CJK run and each hyphenated word such as "CIL-LLM" is one term) and carries no function word of the chunk's language. pipeline/pipeline/retrieval/store.py pipeline/pipeline/retrieval/chunking.py
- Developer Epo decided concept extraction, the `rag_concepts`/`rag_concept_mentions` tables and the search-result concept footer are removed; multi-hop stays with the agent loop over hybrid search. Reopen only when `rag_search_events` shows a measured bridging failure (a two-document question whose cited passage is absent from every search of the turn, or three or more overlapping searches with an uncited answer); the candidate is then the LinearRAG shape (cheap NER/keyphrase extraction, one-hop co-mention expansion as a third RRF leg), not LLM extraction.
- Developer Epo decided a search whose hits are at least half repeats of passages already shown in the turn says so in the result footer and asks the model to report the gap instead of rewording. pipeline/pipeline/retrieval/tools.py
- Developer Epo decided RAG search telemetry is one `rag_search_events` row per `search_workspace` call, written at turn end with which hits the answer cited, storing features and ids only plus `hits_lang` (majority chunk language of the hits, not a detected query language), pruned after 90 days, with no user thumbs signal. server/migrations/0001_init.sql pipeline/pipeline/retrieval/store.py pipeline/pipeline/retrieval/search.py pipeline/pipeline/retrieval/agent.py
- Developer Epo decided the lab sample documents live on the ingest host under /opt/capy-rag-lab/samples/<lang>/ and the retrieval eval harness and question sets live in the repo under bench/rag. bench/rag/scripts/rag_eval.py
- Developer Epo decided Google Drive and OneDrive imports run as an `import` job type in the pipeline's Postgres `jobs` queue on the ingest host, replacing the Cloudflare Worker relay, its Queues, HMAC relay auth, gateway dispatcher and outbox columns. pipeline/pipeline/ingest/import_stage.py pipeline/pipeline/ingest/import_worker.py pipeline/pipeline/jobs.py server/internal/httpapi/internal_import.go server/internal/store/source_imports.go
- Developer Epo decided the provider-to-B2 transfer is its own stage that succeeds before any parse or ingest job exists, takes its own capacity slot, retries retryable errors against the import attempt budget, and fails the import and releases its reservation on a terminal error or exhausted budget. pipeline/pipeline/ingest/import_stage.py pipeline/pipeline/ingest/capacity.py
- Developer Epo decided import downloads are limited to the CAPY_IMPORT_DOWNLOAD_HOSTS allowlist (including `microsoftpersonalcontent.com`), resolve the host, reject private/reserved addresses and connect to the validated address with the hostname as SNI; a Google bearer grant goes only to `www.googleapis.com`. pipeline/pipeline/ingest/pinned_http.py pipeline/pipeline/config.py pipeline/pipeline/ingest/import_stage.py
- Developer Epo decided a provider 401/403 on the import download is terminal (the user re-picks the file); a gateway `too_many_ingest_leases` 429 at completion and the held-lease 409 both return the import claim without spending an attempt and back the queue row off by the gateway's `Retry-After` (remaining lease seconds for the 409); a database error while yielding a claim is retryable; CAPY_IMPORT_JOB_TIMEOUT stays under the gateway's twelve-minute attempt lease; import upload sessions expire after one hour; one import request carries at most 20 files, matching the per-actor ingest lease cap. pipeline/pipeline/ingest/import_stage.py pipeline/pipeline/config.py server/internal/httpapi/internal_import.go server/internal/httpapi/huma_sources.go server/internal/httpapi/apimodel/requests.go
- Developer Epo decided source evidence is the last indexed checkpoint plus exact net changes through the latest durable editing checkpoint, across search, source listing, descriptions, reading and study-material generation; local uncommitted edits and unaccepted AI suggestions stay outside it. pipeline/pipeline/retrieval/pending.py pipeline/pipeline/retrieval/agent.py pipeline/pipeline/retrieval/workflows.py
- Developer Epo decided Office automatic re-ingestion becomes eligible at 5,000 estimated tokens of net changes after 60 seconds without edits (the debounce resets when edits resume, with no maximum wait), below-threshold changes stay retained until enough accumulates or a manual refresh; text/JSON/Markdown/CSV/TSV reindex every 15 seconds with no minimum; indefinite pending changes and chat-context overflow during active editing are accepted; these starting values are subject to testing. collaboration/src/sourceDocuments.ts server/internal/store/source_refresh.go
- Developer Epo decided pending source changes reserve model input tokens and stay outside chat compaction and checkpoint summaries; the chat compaction ceiling is 250,000 tokens. pipeline/pipeline/retrieval/pending.py pipeline/pipeline/retrieval/compact.py
- Developer Epo decided when exact pending changes cannot fit in chat context an information label warns that some information may be outdated while exact changes stay retained durably (separate from the rejected historical-citation source-changed notice); only the workspace owner sees the process-changes-now button and may trigger reprocessing, edit-capable actors see the label only, viewers get nothing because the gateway forwards `pending_sources` only to edit-capable actors with `omitted` always serialized as a boolean; the refresh path keeps its requester/payer plumbing until a separate cleanup decision. pipeline/pipeline/retrieval/pending.py server/internal/httpapi/chat_stream.go src/features/workspace/ChatPanel.tsx src/features/workspace/GeneratePanel.tsx
- Developer Epo decided visual edits keep image-added/changed placeholders without triggering parsing; an agent tool resolves a placeholder by captioning the authoritative image, caching the result and replacing the placeholder with the caption; hyperlink edits contribute text only. vendor/betteroffice/shared/office-checkpoint.ts pipeline/pipeline/retrieval/pending.py pipeline/pipeline/retrieval/tools.py server/internal/store/source_caption.go
- Developer Epo decided image-caption and audio-transcript caches key on asset/file SHA alone, excluding prompt versions; parsed-image captions reuse identical bytes across revisions and surrounding-context changes, retrieval supplying the surrounding chunk alongside the caption; cache payloads are shared physical storage with resource references and authorization-gated access, visibility changes alter reuse eligibility without moving bytes, and authorized reuse attaches references to existing payloads. pipeline/pipeline/parse/caption_cache.py pipeline/pipeline/ingest/source_text.py
- Developer Epo decided editable sources retain only the current file's ingest cache (no dedicated base-file cache for re-upload reuse); uneditable image/audio sources retain their single source cache. server/internal/store/source_refresh.go
- Developer Epo decided deleting a source cancels its scheduled reparse/reindex work; workspace clones use the available projected source snapshot without waiting for newer edits and exclude pending changes. server/migrations/0001_init.sql server/internal/store/share.go
- Developer Epo decided each workspace has auto-reparse and auto-reindex settings, both default true; auto-reparse requires at least one successful prior parse (upload or manual). server/internal/store/queries.go server/internal/store/source_refresh.go src/features/workspace/WorkspaceSettingsDialog.tsx
- Developer Epo decided simple and detailed document summaries regenerate from the complete current file content. pipeline/pipeline/retrieval/indexing.py
- Developer Epo decided text refresh uses the ordinary full-file ingest path with parsing skipped (durable source bytes in B2, temporary ingest-worker copies), reusing existing vectors only on exact embedding input and model identity, otherwise embedding the full file. pipeline/pipeline/ingest/worker.py pipeline/pipeline/retrieval/indexing.py pipeline/pipeline/retrieval/store.py
- Developer Epo confirmed no production data or database exists, so image-only caption generation and privacy rules carry no backwards compatibility or legacy-caption migration. pipeline/pipeline/prompts/captioning.py
- Developer Epo decided Office editing stays available during idle-triggered processing; edits arriving after the processing checkpoint postpone base replacement until a fresh idle checkpoint is processed, keeping the current base and Undo/Redo until a successful current-checkpoint handoff. collaboration/src/sourceHandoff.ts server/internal/store/source_refresh.go src/features/files/useSourceSession.ts
- Developer Epo decided chat compaction and checkpoint summaries target 4,000 to 10,000 tokens with a hard 12,000-token output ceiling. pipeline/pipeline/prompts/chat.py pipeline/pipeline/retrieval/compact.py
- Developer Epo authorized OpenDataLoader-versus-MinerU configuration benchmarks on the ingest VM 159.195.61.195 in isolated containers, excluding standalone Docling, Marker and MarkItDown, assessing suitability for Capy without changing the production parser. bench/parsers/reports/2026-09-08-opendataloader-plan.md
- Developer Epo authorized deeper Java-only OpenDataLoader benchmarks aimed at retaining the extraction speed advantage with comparable study-content quality, including selective OCR and image captions to recover incomplete output; keep production unchanged. bench/parsers/reports/2026-09-08-java-recovery-plan.md
- Developer Epo authorized extending the Java recovery tests with Alibaba Qwen3.8 Flash image captioning through the supplied endpoint and token rates, and testing the recommended recovery strategies while keeping production unchanged. bench/parsers/reports/2026-09-08-qwen-java-recovery-plan.md
- Developer Epo authorized implementing and benchmarking better recovery-page selection, table-header preservation and figure-aware chunking, plus related content-quality improvements; use Qwen3.8 Flash for subsequent captioning experiments. bench/parsers/reports/2026-09-09-structural-recovery-plan.md bench/parsers/scripts/bench_java_recovery.py bench/parsers/scripts/structured_recovery.py
- Developer Epo authorized iterative MinerU native-text/geometry model-bypass experiments on the ingest VM with Astra xhigh subagents, comparing the best measured candidate with improved Java structural/Qwen recovery while preserving production parsing. bench/parsers/reports/2026-09-09-mineru-native-plan.md bench/parsers/scripts/run_mineru_native.py bench/parsers/scripts/mineru_native_text.py bench/parsers/scripts/mineru_native_page.py bench/parsers/scripts/replay_java_page_cache.py bench/parsers/reports/2026-09-09-mineru-native-geometry.md
- Developer Epo authorized sourcing new documents and testing the recommended Java recovery improvements in isolated benchmarks, including an Astra high Qwen3.5-OCR evaluation, then paused Beijing endpoint tests pending regional business validation. bench/parsers/reports/2026-09-09-java-new-documents-plan.md bench/parsers/scripts/prepare_new_parser_corpus.py bench/parsers/scripts/structured_recovery.py bench/parsers/reports/2026-09-09-beijing-ocr.md
- Developer Epo authorized comparing current refined OpenDataLoader and existing MinerU through the complete agentic RAG flow, reusing and extending bench/rag questions and testing ODL retrieval adjustments in isolation; use Alibaba Qwen3.8 Flash for answering and captioning and DeepInfra Qwen3-Embedding-4B for embeddings, while preserving production behavior. bench/rag/scripts/odl_agentic_run.py
