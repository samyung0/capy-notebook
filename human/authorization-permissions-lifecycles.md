- Developer Epo decided missing, expired, revoked, deleted, deletion-pending, suspended, over-quota or otherwise unavailable workspace invitations use one non-disclosing unavailable-workspace UI even when email was sent; authoritative acceptance checks control access and queued email needs no retraction path. server/internal/store/collaboration.go server/internal/httpapi/huma_collaboration.go src/routes/WorkspaceInviteAccept.tsx
- Developer Epo decided the anonymous workspace summary page keeps the permanent `ws_` URL with the existing workspace id (analytics tracing, bookmarks, SEO; revoking means setting privacy to private) and exposes workspace metadata plus the chapter/file outline (files as name, sizeBytes, addedAt only) in one reading column with client-side authentication controls in the top inset bar; full workspace and study content require authentication. server/internal/store/workspace_summary.go workers/site/summary.ts
- Developer Epo decided image captions contain only image-derived information and reuse follows containing-resource privacy: private workspace images reuse within that workspace, private standalone quiz/flashcard images reuse for the same user's copies, and link-shared/public images of either kind permit global reuse; identical image bytes are the content match and different MinerU crops may be captioned independently. pipeline/pipeline/parse/caption_cache.py pipeline/pipeline/prompts/captioning.py server/internal/store/share.go
- Developer Epo decided workspace chat is open to any signed-in actor holding an effective role, billed to the actor's own credits; anonymous visitors stay summary-only; conversations stay private per user and become unreadable after privacy revocation rather than deleted; the create_material tool is omitted from the chat tool prompt and the create-material endpoints reject when the actor cannot generate. server/internal/store/chat.go server/internal/httpapi/chat_stream.go server/internal/httpapi/huma_materials.go server/internal/httpapi/internal_materials.go pipeline/pipeline/retrieval/tools.py pipeline/pipeline/retrieve/service.py
- Developer Epo decided workspace roles are owner, editor, viewer for members and editor, viewer for link/public share; the commenter tier is removed from schema, API, collaboration server, UI, copy, seeds and tests with no data migration because no production data exists. server/internal/store/enums.go server/migrations/0001_init.sql collaboration/src/persistence.ts src/features/workspace/ShareDialog.tsx
- Developer Epo decided the effective role is max(membership, share role) and member editors and link/public editors hold identical active-content authority (document body, comments, chapters, file upload/rename/move/trash, material metadata and trash, generation, workspace clone); shared trash management belongs to the workspace owner; bytes are charged to the workspace owner and inference credits to the actor. server/internal/store/share.go server/internal/store/storage.go server/internal/store/source_imports.go server/internal/httpapi/server.go server/internal/httpapi/huma_materials.go
- Developer Epo decided workspace settings (name, color, description, tags, privacy, share role, stats) require persisted owner or editor membership; membership management, delete and transfer stay owner-only; the owner's lifecycle gates only exposure widening (privacy toward public or share role toward editor) whoever the actor is, and narrowing is a recovery action an over-quota owner may always perform. server/internal/store/queries.go server/internal/httpapi/huma_workspaces.go src/features/workspace/access.ts src/features/workspace/WorkspaceSettingsDialog.tsx
- Developer Epo decided viewers, member or link/public, read, chat and download only: no comments, no collaboration token, static rendering; the editor runtime has no canComment flag and comment affordances key on canEdit. server/internal/httpapi/huma_collaboration.go collaboration/src/persistence.ts src/features/materials/modePolicy.ts src/features/notes/EditorRuntime.tsx
- Developer Epo decided comment authors edit and delete their own comments and discussions; only the workspace owner deletes another user's. server/internal/httpapi/huma_collaboration.go
- Developer Epo decided workspace clone requires membership of any role or an effective editor grant (link/public viewers download files instead, keeping cloning of large workspaces rare), while standalone quiz, flashcards and material clone stays open to every signed-in reader on link or public visibility; the Go server computes canClone from the caller's real grant on every workspace response including Explore rows, and the clone action renders in the workspace card action menu and the General tab of the settings dialog only when canClone is true. server/internal/store/share.go server/internal/httpapi/apimodel/apimodel.go src/features/workspace/access.ts src/routes/Explore.tsx src/components/ui/WorkspaceCard.tsx src/features/workspace/WorkspaceSettingsDialog.tsx
- Developer Epo decided API responses never carry member emails; rosters and mention directories show id, display name and avatar, showing the user id when the name is empty, and comment author snapshots stay as stored. server/internal/store/collaboration.go server/internal/store/models.go src/features/workspace/WorkspaceMemberManager.tsx
- Developer Epo decided refused workspace settings, statistics and clone requests answer non-disclosing 404 like every other workspace mutation, while comment and collaboration-token refusals answer 403; the member roster answers 404 when the caller has no read access and 403 for a reader who is not a member. server/internal/httpapi/huma_register.go server/internal/httpapi/huma_membership.go e2e/sharing/workspace-sharing.spec.ts
- Developer Epo decided (2026-09-09) manual and agent deletion of source files and Plate materials first enters a 30-day trash stage; only the current workspace owner can list, restore or permanently delete shared trash, regardless of who trashed it; standalone material trash belongs to its owner; the Files page gains a Trash tab using the existing Tabs component. artifacts/agent-tools/plan.md server/internal/store/trash.go server/internal/httpapi/huma_trash.go server/cmd/api/trash_worker.go src/routes/Files.tsx pipeline/pipeline/retrieval/tools.py
- Developer Epo decided (2026-09-09) the trash boundary is enforced by an explicit trashed-state filter on every active-resource query in Go, Python and the collaboration service, mapped site by site (no view-based soft-delete layer over files/materials), while maintenance, quota, refcount and trash paths read trashed rows deliberately; Office (DOCX/XLSX/PPTX) direct editing ships in the same pass as Plate/text editing through BetterOffice fork work. server/migrations/0001_init.sql server/internal/store/queries.go collaboration/src/persistence.ts pipeline/pipeline/store/db.py vendor/betteroffice/shared/office-checkpoint.ts collaboration/src/editCommands.ts collaboration/src/sourceDocuments.ts
- Developer Epo decided (2026-09-10) `users.name` is user-owned: Clerk profile sync and the Clerk webhook write it only when inserting the local account (provider name, else email, else the localized fallback), later syncs refresh only email and avatar, `PATCH /api/me` stores the trimmed 1-60 rune name typed by the user, and Stripe customers carry that same name because no legal name is stored. server/internal/store/users.go server/internal/httpapi/huma_account.go server/internal/httpapi/webhooks.go
- Developer Epo decided (2026-09-11) browsers read source and citation-preview bytes through one authenticated links endpoint returning short-lived presigned B2 URLs; the redirecting raw/preview routes, inline files.content and external url branches are removed. server/internal/httpapi/huma_content.go server/internal/store/jobs.go src/api/hooks.ts src/features/files/FileViewer.tsx
- Developer Epo decided (2026-09-11) the files.url column is dropped by a new migration; file rows expose a hasBytes boolean computed from blob_path in fileCols, the browser holds only the short-lived presigned URL from /links, fetched by each consumer when it mounts and used at fetch time only (expiresAt is ignored client-side; no expiry, reconnect, focus or file-row-change refetch, so a mounted view never sees a changed URL), the Office citation preview resolves its own pair when it opens, a fetch or media failure on an expired URL renders the retryable file error whose retry fetches a fresh pair, read links sign with B2_LINK_TTL (default 300 s, matching the workspace-summary edge cache) while B2_PRESIGN_TTL (900 s) stays on upload PUTs because it doubles as the reservation deadline that completion checks, and MSW mock rows carry hasBytes with bodies served by the mock links handler. server/migrations/0002_drop_files_url.sql server/internal/store/queries.go server/internal/blob/s3.go server/cmd/api/main.go src/api/hooks.ts src/features/files/FileViewer.tsx src/mocks/db.ts src/mocks/handlers.ts
- Developer Epo decided (2026-09-11) the unused files.content column is dropped by a new migration and the unsupported-file preview text is localized. server/migrations/0003_drop_files_content.sql src/features/files/FileViewer.tsx
- Developer Epo decided (2026-09-11) a presigned read URL is consumed at the moment it is minted: links that a user clicks later (unsupported-file download, Plate file attachments) resolve on click, and Plate audio re-resolves on a media error, so the 300 s link TTL never surfaces as a dead link. src/features/files/FileViewer.tsx src/features/materials/MediaAssetView.tsx
