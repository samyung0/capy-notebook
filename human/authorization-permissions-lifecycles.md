- Developer Epo decided missing, expired, revoked, deleted, deletion-pending, suspended, over-quota, or otherwise unavailable workspace invitations use one non-disclosing unavailable-workspace UI even when email was sent; authoritative acceptance checks control access and queued email needs no retraction path. server/internal/store/collaboration.go server/internal/httpapi/huma_collaboration.go src/routes/WorkspaceInviteAccept.tsx
- Developer Epo decided (2026-09-05) public workspace summary links retain existing workspace IDs for direct analytics tracing; summary pages expose workspace metadata and chapter/file names while full workspace and study content require authentication.
- Developer Epo selected (2026-09-05) workspace summary layout A: one reading column with workspace metadata above the chapter/file outline, matching the existing product styling and showing client-side authentication controls in the top inset bar.
- Developer Epo decided (2026-09-06) image captions contain only image-derived information and reuse follows containing-resource privacy: private workspace images reuse within that workspace, link-shared/public workspace images permit global reuse, private standalone quiz/flashcard images reuse for the same user's copies, and link-shared/public standalone images permit global reuse; identical image bytes remain the content match, and different MinerU crops may be captioned independently. pipeline/pipeline/parse/caption_cache.py pipeline/pipeline/prompts/captioning.py pipeline/pipeline/retrieval/store.py server/internal/store/share.go
