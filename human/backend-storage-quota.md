- Developer Epo decided each single-material clone is a private standalone material with fresh clone-owned logical asset IDs; current and retained content rewrite copied asset references, uncopied media is removed, clone bytes charge the cloner, and physical objects stay shared through blob reference counting. server/internal/store/share.go server/internal/materialdoc/document.go server/migrations/0001_init.sql
- Developer Epo decided plan limits are Free/Pro storage 100 MB/1 GB and source files 10 MiB/30 MiB; both plans allow unlimited owned workspaces, 100 files per workspace and 20 files per upload/import request. server/migrations/0001_init.sql server/internal/planlimits/catalog.go src/features/billing/planLimits.ts
- Developer Epo decided (2026-09-09) materials keep no snapshot history: the daily material revision table, its plan limit, listing route, clone copy and downgrade pruning are removed destructively (no production data); materials.revision stays as the content version counter. server/migrations/0001_init.sql server/internal/store/queries.go server/internal/httpapi/huma_materials.go
- Developer Epo decided (2026-09-11) a material title rename is last write wins like a file rename: the metadata PATCH carries no expectedRevision, a title-only patch leaves revision untouched, and the tree's MaterialRef carries no revision; the tree trigger stays silent on revision/size/node-count changes so collaboration checkpoints raise no tree event. server/internal/httpapi/huma_materials.go server/internal/httpapi/apimodel/requests.go server/internal/store/queries.go server/internal/store/models.go src/features/workspace/ContentActions.tsx
- Developer Epo decided (2026-09-09) a material edit's stored inverse plus guards is capped at 256 KB and refused as quota_rejected above it; the owner is charged the stored bytes. collaboration/src/persistence.ts
- Developer Epo decided plan-independent write limits are 2 MiB normalized material JSON, 10,000 material nodes, depth 16, 20 MB editor images and 100 MB editor audio. server/internal/materialdoc/document.go server/internal/httpapi/editor_assets.go src/lib/const.ts
- Developer Epo decided (2026-09-09) trashed content keeps counting toward owner storage and source-file count limits until permanent deletion; restore reuses the retained resource and charge. artifacts/agent-tools/plan.md
- Developer Epo decided text field limits in runes: workspace name 80 and description 500, chapter name 60, file name 120 on every path (upload, import, rename, editor asset), material/quiz/flashcard/canvas/generate title 120, conversation title 60, event title 60 and location 100, task title 80, label name 35, tag value 35, email 254, profile name 60. server/internal/fieldlimits/fieldlimits.go
- Developer Epo decided Go constants are the single source of truth for field limits: huma request fields use named string types whose SchemaTransformer sets MaxLength so OpenAPI, orval validators and the generated Python limits (only MATERIAL_TITLE_MAX) derive from them, Postgres CHECK constraints carry the same literals with a Go test asserting pg_constraint matches, browser zod validators may count UTF-16 units and stay stricter than the rune-counting API, and uploads go through the XHR progress client rather than the orval multipart client. server/internal/fieldlimits/fieldlimits.go server/internal/httpapi/apimodel/limits.go server/internal/store/fieldlimits_test.go server/cmd/openapi/main.go pipeline/pipeline/generated/limits.py src/api/hooks.ts
- Developer Epo decided over-limit file names typed by the user are rejected (the source details dialog keeps the row visible with an error border plus InputError and blocks submit; API routes reject through schema validation), while values the user did not type are clamped, extension-preserving for file names: Clerk profile names, LLM-authored material titles, editor asset names, the workspace clone " (copy)" suffix and auto-derived conversation titles; an oversized Clerk email is dropped so the stored email is kept. src/features/workspace/AddSourceDialog.tsx server/internal/store/users.go server/internal/httpapi/internal_materials.go server/internal/httpapi/editor_assets.go server/internal/store/share.go server/internal/httpapi/chat_stream.go
