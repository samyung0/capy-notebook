- Developer Epo decided each single-material clone is a private standalone material with fresh clone-owned logical asset IDs; current and retained content rewrite copied asset references, uncopied media is removed, clone bytes charge the cloner, and physical objects remain shared through blob reference counting. server/internal/store/share.go server/internal/materialdoc/document.go server/migrations/0001_init.sql
- Developer Epo decided plan limits are Free/Pro storage 100 MB/1 GB, source files 10 MiB/30 MiB, and daily material revisions 3/30; both plans allow unlimited owned workspaces, 100 files per workspace, and 20 files per upload/import request. server/migrations/0001_init.sql server/internal/planlimits/catalog.go src/features/billing/planLimits.ts
- Developer Epo decided a Pro-to-Free plan change retains only the newest material snapshots allowed by Free and permanently discards every excess snapshot. server/internal/store/material_revisions.go server/internal/store/subscriptions.go
- Developer Epo decided plan-independent write limits are 2 MiB normalized material JSON, 10,000 material nodes, depth 16, 20 MB editor images, and 100 MB editor audio. server/internal/materialdoc/document.go server/internal/httpapi/editor_assets.go src/lib/const.ts
- Developer Epo decided (2026-09-07) text field limits in runes: workspace name 80 and description 500, chapter name 60 (including upload/import chapterName), file name 120 on every path (upload, import, rename, editor asset), material/quiz/flashcard/canvas/generate title 120, conversation title 60, event title 60 and location 100, task title 80, label name 35, tag value 35, email 254 (invite identifier, deletion confirm, users.email), profile name 60. server/internal/fieldlimits/fieldlimits.go server/internal/httpapi/apimodel/limits.go server/migrations/0001_init.sql
- Developer Epo decided (2026-09-07) Go constants are the single source of truth for field limits: huma request fields use named string types whose SchemaTransformer sets MaxLength so OpenAPI, orval validators, and the generated Python limits derive from them; Postgres CHECK constraints carry the same literals and a Go test asserts pg_constraint matches the constants; hand-written length checks are removed. server/internal/fieldlimits/fieldlimits.go server/internal/httpapi/apimodel/limits.go server/internal/store/fieldlimits_test.go server/cmd/openapi/main.go pipeline/pipeline/generated/limits.py
- Developer Epo decided (2026-09-07) file names over the limit are rejected on upload and import: the unified source details dialog keeps the row visible with an error border plus InputError and blocks submit, and the API routes reject through schema validation; values the user did not type are clamped, extension-preserving for file names: Clerk profile names, LLM-authored material titles, editor asset names, the workspace clone " (copy)" suffix, and auto-derived conversation titles. src/features/workspace/AddSourceDialog.tsx server/internal/store/users.go server/internal/httpapi/internal_materials.go server/internal/httpapi/editor_assets.go server/internal/store/share.go server/internal/httpapi/chat_stream.go
- Developer Epo decided (2026-09-08) the pipeline receives only MATERIAL_TITLE_MAX from the generated limits, an oversized Clerk email is dropped so the stored email is kept, browser zod validators may count UTF-16 units and stay stricter than the rune-counting API, and the orval-generated multipart uploadSource client stays unused because uploads go through the XHR progress client. server/cmd/openapi/main.go server/internal/store/users.go src/api/hooks.ts
