- Developer Epo decided UAT runs on its own Coolify instance on the Netcup VM `159.195.250.206` (Debian 13, 2 vCPU, 3.8 GB; SSH `root` with `~/.ssh/id_ed25519_evo_uat`), separate from the Hetzner production Coolify host; the UAT resource is created from `deploy/docker-compose.prod.yml` and driven through the Coolify API by `deploy-environment.yml`. `deploy/ansible/app-host/inventory.yml`, `.github/workflows/deploy-environment.yml`
- Developer Epo decided UAT hostnames are single-level subdomains of `capynotebook.com` because the free Universal certificate covers only `*.capynotebook.com`: `uat.capynotebook.com` (SPA, Cloudflare Pages project `evo-notes-uat`), `uat-api`, `uat-collab`, `uat-ops`, and `uat-coolify` (Coolify UI/API), all through Cloudflare tunnel `evo-uat` to the VM; the origin drops public 80/443/8000/6001/6002/8080 via a `DOCKER-USER` rule and reaches the ingest host over WireGuard as `10.77.0.3`. `deploy/.env.uat.example`, `openwiki/deployment-runbook.md`
- Developer Epo decided UAT uses a separate Clerk application `Evo Notes UAT`, the Stripe sandbox account `Stable Studio sandbox` (`acct_1U8Djl2ZZopeANOe`; the `Stable Studio` test-mode data is retired), and a dedicated B2 UAT bucket created by hand.
- Developer Epo decided local scanner and deployment secrets (Coolify API token, LLM key, Strix instructions) live only in the ignored `deploy/.env.uat`; agents read them from there and never print them. `scripts/review/common.sh`
- Developer Epo decided the UAT Coolify dashboard is reached only through the `evo-uat` Cloudflare tunnel at `uat-coolify.capynotebook.com` routed to Traefik on port 80, with the Coolify instance FQDN stored as `http://` so Traefik skips Let's Encrypt, `APP_URL` set to `https://` so asset URLs are not blocked as mixed content, and zone setting Always Use HTTPS on; the tunnel hostname carries the `/app` realtime websocket and `/terminal/ws`.
- Developer Epo decided Coolify holds one environment per project on the UAT instance (`uat` only; the auto-created empty `production` environment is deleted because production lives on its own VM) and keeps no preview-deployment variable set, since preview deployments stay disabled. `deploy/docker-compose.prod.yml`
- Developer Epo decided every compose variable Coolify has not been given a real value is stored blank so the `${VAR:?message}` guards fail closed, because Coolify otherwise stores the guard message itself as the value and the gateway starts with credentials like the literal `set CLERK_SECRET_KEY`. `openwiki/deployment-runbook.md`
- Developer Epo decided generated UAT deployment secrets are readable in Coolify rather than marked shown-once, and are mirrored in the ignored `deploy/.env.uat`, because the ingest host queue env and `OPS_INGEST_UAT_DATABASE_URL` need the Postgres password in cleartext. `deploy/.env.uat.example`
- Developer Epo decided every environment's variables live in one file under `deploy/` (`.env` for local, `.env.uat`, `.env.prod`) instead of a `.env` per component, for development and CI simplicity: one file to copy, one place for a value to be wrong. `vite.config.ts` reads it through `envDir`, `playwright.config.ts` and the collaboration sidecar load it by path, docker compose auto-loads it, and the Go and Python processes are exported from it by hand. `deploy/.env.example`, `vite.config.ts`
- Developer Epo decided developers run the SPA locally against a deployed environment on a hostname one label under that environment's Clerk primary domain (`dev-<name>.uat.capynotebook.com`), covered by an Advanced Certificate Manager pack on `capynotebook.com`, because Clerk production instances share sessions across subdomains of the primary domain by default and a sibling hostname would need per-developer satellite registration plus a permanent `isSatellite` branch in the SPA. `scripts/dev/tunnel.sh`, `deploy/b2-cors.uat.json`, `src/components/app/AuthProvider.tsx`
- Developer Epo decided backend and schema work keeps a second lane with the gateway, Postgres and Redis local on the Clerk development instance, because the migrator records a checksum per file and refuses to run when an applied one changes, so schema work needs a database the developer can drop. `deploy/docker-compose.yml`, `server/cmd/api/main.go`
- Developer Epo decided (2026-09-05) the `workers/drive-import-relay` Cloudflare app is deleted; the ingest host reaches the gateway's `/api/internal/import/*` routes with `GATEWAY_URL` and `PIPELINE_SECRET`, and production publishes the gateway on the WireGuard address next to Postgres and Redis. deploy/docker-compose.prod.yml deploy/docker-compose.ingest-host.yml deploy/ingest-host.env.example
- Developer Epo decided (2026-09-05) the Pages deploy pins wrangler to 4.125.0 and the import download host allowlist has one default in `pipeline/pipeline/config.py` with `EVO_IMPORT_DOWNLOAD_HOSTS` as the only override, not a compose literal. .github/workflows/deploy-environment.yml deploy/docker-compose.ingest-host.yml
- Developer Epo decided (2026-09-05) Sentry and PostHog both sit in the EU region, replacing the empty US `stablestudio` Sentry organization with a new EU one, because a Sentry organization's storage location is fixed at creation and a PostHog region move needs a paid-tier support ticket; GDPR reaches the product through its EU users rather than through where the company is registered, so EU hosting retires the Chapter V transfer question instead of satisfying a residency rule. deploy/.env.example
- Developer Epo decided (2026-09-05) Sentry starts as two projects, `capy-web` for the browser SPA and `capy-backend` shared by gateway, retrieval, worker, collaboration and the ingest-host consumers which separate by their existing `service` tag, with `capy-ops` added when the ops profile is enabled so operator alerts stay off the product rules. deploy/.env.example deploy/docker-compose.prod.yml
- Developer Epo decided (2026-09-05) PostHog stays keyless in local and UAT and its project is created at production launch, verified once beforehand with a throwaway key, because nothing in the app depends on it: a missing key leaves capture, identify and flags inert. src/lib/observability.ts
- Developer Epo decided (2026-09-05) GitHub environment variables and secrets are the deployment source of truth; local environment files provide a convenient authorized upload path, and workflows dispatch UAT Coolify deployments and synchronize environment configuration.
- Developer Epo decided (2026-09-05) each manual deployment applies its GitHub environment configuration and app/ingest deployment use separate workflows.
- Developer Epo decided (2026-09-05) one Cloudflare Worker serves the static SPA and server-rendered workspace summaries supplied live by Go, superseding Pages hosting; defer KV/R2 summary caching.
- Developer Epo approved (2026-09-05) a dedicated SSH key for GitHub UAT deployments as capy-ingest on 159.195.61.195, with the private key stored in the GitHub uat environment and ignored mode-0600 deploy/.env.uat. .github/workflows/deploy-ingest.yml scripts/deploy/ingest-host-release.sh
- Developer Epo approved (2026-09-05) performing the Cloudflare hosting changes and using uat-office.capynotebook.com for the isolated UAT Office runtime. workers/office/index.ts wrangler.office.jsonc .github/workflows/deploy-environment.yml
- Developer Epo requested (2026-09-05) repeatable UAT seed initialization for the dedicated accounts and authorization fixture, and approved capy-uat-{owner,editor,commenter,viewer,other}+clerk_test@stablestudio.org as the five account emails. scripts/uat/seed.py scripts/uat/seed.sql
- Developer Epo requested (2026-09-05) implementing support for future database baselines now, using the design discussion in the Codex task "Review database migration strategy", before any further deployments. server/internal/store/migrate.go server/internal/store/migration_plan.go server/cmd/migrate/main.go
- Developer Epo decided (2026-09-06) to standardize ops endpoint configuration on environment-scoped DEPLOYMENT_OPS_URL, remove UAT_OPS_URL and its fallback, and finish active Capy rename configuration cleanup without deploying. .github/workflows/uat-quality.yml deploy/env-manifest.json scripts/review/common.sh scripts/review/remote-smoke.sh scripts/env/config.py
- Developer Epo decided (2026-09-07) the pinned BetterOffice capy-ci commit is authoritative for Binaryen; Capy's collaboration Docker build reads the version and checksum from that checkout's wasm-toolchain action instead of maintaining a separate optimizer pin. collaboration/Dockerfile vendor/betteroffice/.github/actions/wasm-toolchain/action.yml
