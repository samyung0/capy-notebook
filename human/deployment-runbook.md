- Developer Epo decided UAT runs on its own Coolify instance on the Netcup VM 159.195.250.206 (Debian 13, 2 vCPU, 3.8 GB; SSH root with ~/.ssh/id_ed25519_capy_uat), separate from the Hetzner production Coolify host; the UAT resource is the `deploy/docker-compose.prod.yml` compose driven through the Coolify API by `deploy-environment.yml`, inside one Coolify project holding the single `uat` environment (the auto-created `production` environment is deleted) with preview deployments disabled and no preview variable set. scripts/uat/seed.py .github/workflows/deploy-environment.yml deploy/docker-compose.prod.yml
- Developer Epo decided UAT hostnames are single-level subdomains of capynotebook.com because the free Universal certificate covers only `*.capynotebook.com`: `uat` (SPA Worker) and `uat-office` (isolated Office runtime Worker) on Cloudflare Workers, and `uat-api`, `uat-collab`, `uat-ops`, `uat-coolify` through the `capy-uat` tunnel to the VM; the origin drops public 80/443/8000/6001/6002/8080 with a DOCKER-USER rule and binds private services on its WireGuard address 10.77.0.3 peered with the ingest host. deploy/.env.uat.example wrangler.jsonc wrangler.office.jsonc
- Developer Epo decided the UAT Coolify dashboard is reached only through the tunnel at uat-coolify.capynotebook.com routed to Traefik on port 80, with the Coolify instance FQDN stored as `http://` so Traefik skips Let's Encrypt, `APP_URL` as `https://` so assets are not mixed content, zone setting Always Use HTTPS on, and the tunnel hostname carrying the `/app` realtime websocket and `/terminal/ws`.
- Developer Epo decided UAT uses a separate Clerk application `Capy Notebook UAT`, the Stripe sandbox `Stable Studio UAT` (acct_1U8Djl2ZZopeANOe), a hand-created B2 bucket `capy-notebook-uat`, the EU Sentry organization `stable-studio` with projects `capy-web` (SPA) and `capy-backend` (gateway, retrieval, worker, collaboration and ingest-host consumers separated by their `service` tag) plus `capy-ops` once the ops profile is enabled, and EU PostHog kept keyless in local and UAT with the production key wired at launch since a missing key leaves capture, identify and flags inert. deploy/.env.example deploy/docker-compose.prod.yml src/lib/observability.ts
- Developer Epo decided every compose variable Coolify has no real value for is stored blank so the `${VAR:?message}` guards fail closed (Coolify otherwise stores the guard message as the value), and generated UAT secrets stay readable in Coolify rather than shown-once and are mirrored in the ignored `deploy/.env.uat` because the ingest host queue env and `OPS_INGEST_UAT_DATABASE_URL` need the Postgres password in cleartext. deploy/.env.uat.example
- Developer Epo decided GitHub environment variables and secrets are the deployment source of truth, synchronized from the local env file by `env:check`/`env:push` under `deploy/env-manifest.json`; each manual deployment applies its GitHub environment configuration; app, ingest and ops deployments are separate workflows; the ops endpoint is the environment-scoped `DEPLOYMENT_OPS_URL`. deploy/env-manifest.json scripts/env/config.py .github/workflows/deploy-environment.yml .github/workflows/deploy-ingest.yml .github/workflows/deploy-ops.yml scripts/review/common.sh
- Developer Epo decided local scanner and deployment secrets (Coolify API token, LLM key, Strix instructions, Cloudflare token) live only in the ignored `deploy/.env.uat`; agents read them from there and never print them. scripts/review/common.sh
- Developer Epo decided every environment's variables live in one file under `deploy/` (`.env` local, `.env.uat`, `.env.prod`) rather than a file per component: `vite.config.ts` reads it through `envDir`, Playwright and the collaboration sidecar load it by path, docker compose auto-loads it, and the Go and Python processes are exported from it by hand. deploy/.env.example vite.config.ts
- Developer Epo decided developers run the SPA locally against a deployed environment on `dev-<name>.uat.capynotebook.com`, one label under that environment's Clerk primary domain and covered by an Advanced Certificate Manager pack, because Clerk shares sessions across subdomains of the primary domain while a sibling hostname would need per-developer satellite registration and an `isSatellite` branch in the SPA. scripts/dev/tunnel.sh deploy/b2-cors.uat.json src/components/app/AuthProvider.tsx
- Developer Epo decided backend and schema work keeps a second lane with the gateway, Postgres and Redis local on the Clerk development instance, because the migrator records a checksum per file and refuses an applied file that changed, so schema work needs a database the developer can drop; the migrator supports database baselines. deploy/docker-compose.yml server/cmd/api/main.go server/internal/store/migrate.go server/internal/store/migration_plan.go server/cmd/migrate/main.go
- Developer Epo decided the `workers/drive-import-relay` Cloudflare app is deleted; the ingest host reaches the gateway's `/api/internal/import/*` routes over WireGuard with `GATEWAY_URL` and `PIPELINE_SECRET`, and the import download host allowlist has one default in `pipeline/pipeline/config.py` with `CAPY_IMPORT_DOWNLOAD_HOSTS` as the only override. deploy/docker-compose.prod.yml deploy/docker-compose.ingest-host.yml deploy/ingest-host.env.example
- Developer Epo decided one Cloudflare Worker serves the static SPA and server-rendered workspace summaries supplied live by Go, replacing Pages hosting, with KV/R2 summary caching deferred; Worker deploys pin wrangler 4.125.0. workers/site/index.ts workers/site/summary.ts .github/workflows/deploy-environment.yml
- Developer Epo decided GitHub UAT ingest deployments use a dedicated SSH key as `capy-ingest` on 159.195.61.195, stored in the GitHub `uat` environment secret and the ignored mode-0600 `deploy/.env.uat`. .github/workflows/deploy-ingest.yml scripts/deploy/ingest-host-release.sh
- Developer Epo decided UAT seed initialization is repeatable for the dedicated accounts and the authorization fixture, with account emails `capy-uat-{owner,editor,viewer,other}+clerk_test@stablestudio.org`. scripts/uat/seed.py scripts/uat/seed.sql
- Developer Epo decided the pinned BetterOffice `capy-ci` commit is authoritative for Binaryen: the collaboration Docker build reads the version and checksum from that checkout's wasm-toolchain action. collaboration/Dockerfile vendor/betteroffice/.github/actions/wasm-toolchain/action.yml
- Developer Epo decided CI and SPA deployment cache compiled BetterOffice WASM keyed by the source/toolchain fingerprint plus runner OS and architecture, keeping `office:prepare` validation and rebuilding CSS, checkpoint bundles and final Vite output per run. .github/actions/cache-betteroffice/action.yml .github/workflows/ci.yml .github/workflows/deploy-environment.yml
