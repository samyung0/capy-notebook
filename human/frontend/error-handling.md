- Developer Epo approved the SSE review fixes: ingest uses the workspace page's file list with its primary error policy, reconciles on connect, and refreshes stale file details when list reads observe completion; failed Files previews display an inline error with manual retry. src/api/hooks.ts src/routes/WorkspaceOpen.tsx src/routes/Files.tsx src/features/files/FileStates.tsx
- Developer Epo decided (2026-09-11) the browser keeps no polling fallback for notifications, files or materials: the single events stream reconnects with backoff, reconciles by refetching the notification, file, material and chapter lists on every connect, coalesces a burst of tree events with a 300 ms trailing debounce per kind (chapters refetched alongside), only marks lists stale in a hidden tab, carries client-only ingest progress across list reads, and the workspace lists keep only refetch-on-focus; a failed connection shows the connection banner while a clean stream end reconnects silently. src/api/hooks.ts src/api/eventStream.ts src/components/app/AppShell.tsx src/components/app/ConnectionBanner.tsx

- Developer Epo requested (2026-09-13) UAT cloud import fixes: give Google Picker exclusive focus and pointer interaction, prevent duplicate picker launches, enable multiple file selection, and report picker failures to Sentry. src/features/workspace/AddSourceDialog.tsx src/lib/googlePicker.ts
- Developer Epo requested (2026-09-15) broader MSW frontend user scenarios, reachable sign-in/sign-up/password-reset pages, and developer buttons opening real dialogs with dummy data when normal mock flows cannot reach them. src/mocks/scenarios.ts src/components/dev/MockScenarioPanel.tsx
- Developer Epo requested (2026-09-22) MSW chat history and User scenarios include the OpenUI designs and failure states for UI development through the real chat renderer and stream parser. src/mocks/chatFixtures.ts src/mocks/chatStream.ts src/mocks/scenarios.ts src/mocks/db.ts

- Developer Epo requested (2026-09-16) Biology 101 file-state fixtures reachable as ordinary files, PDF failures using FileError, and an ownership-transfer preview without an underlying mock member dialog. src/mocks/dialogFiles.ts src/features/files/PdfView.tsx src/components/dev/MockDialogPreview.tsx

- Developer Epo decided (2026-09-16) ErrorDescription carries an optional icon typed IconName rendered only by ErrorState boundaries (toasts keep their variant icon, the share route keeps the default): offline wifiOff, network wifiError, sourceChanged fileError, forbidden securityWarning, server error. src/lib/errors.ts src/components/ui/Icon.tsx src/components/app/ErrorState.tsx src/components/app/AppErrorBoundary.tsx
- Developer Epo requested (2026-09-16) dropdown scenarios for every request-reachable ErrorKind: plain 403 and a rejected fetch on the files list, provider_busy, llm_key_failed and files_batch_exceeded; failure status accepts network for a rejected fetch. src/mocks/scenarioFailures.ts
- Developer approved (2026-09-17) Button asChild slotting through Slot.Slottable, supporting links with or without icons and preventing Dashboard route crashes; requested both icons stay inside the element receiving button classes and props, matching native button mode. src/components/ui/Button.tsx
- Developer requested (2026-09-17) matching workspace-list loader and page query parameters so prefetch and mount share one cache entry. src/router.tsx src/routes/Workspaces.tsx
- Developer requested (2026-09-17) a five-minute frontend React Query default stale time. src/api/queryClient.ts
- Developer requested (2026-09-18) five-second stale times for notifications and workspace chapter, file, and material lists, including shared query options used for route prefetches. src/api/hooks.ts
- Developer approved (2026-09-18) five-second stale times for file and material detail queries while retaining disabled focus refetching to avoid unnecessary editor downloads and viewer reloads. src/api/hooks.ts

- Developer Epo approved (2026-09-23) standard file-preview errors with manual loader retries for Office, CSV/text and images, host-owned Office runtime error presentation, shared warning banners preserving editor recovery actions and PDF content on annotation write errors, and panel errors for workspace statistics/indexing. src/features/files/FileStates.tsx src/features/files/FileViewer.tsx src/features/files/useOfficeRuntime.ts src/components/banners/WarningBanner.tsx src/features/workspace/WorkspaceSettingsDialog.tsx

- Developer Epo decided (2026-09-23) error action buttons default to ghost-hover with a left Hugeicon: refresh for retry/reload, with semantic overrides for navigation, download and discard; audit their error containers for reachability through MSW User scenarios. src/components/ui/Button.tsx src/components/app/AppErrorBoundary.tsx src/features/files/FileStates.tsx src/components/dev/MockScenarioPanel.tsx

- Developer Epo requested (2026-09-23) MSW file/material error launchers open real workspace URLs, using Biology 101 fixtures and appending seeded error materials where needed, rather than file preview dialogs. src/components/dev/MockScenarioPanel.tsx src/mocks/errorMaterials.ts src/mocks/db.ts src/mocks/handlers.ts
- Developer Epo requested (2026-09-23) 4px extra icon-to-label spacing on error-display action buttons through iconLeftClassName props; keep shared Button, ErrorAction and Sonner spacing unchanged. src/features/files/FileStates.tsx src/components/app/AppErrorBoundary.tsx src/routes/WorkspaceInviteAccept.tsx
- Developer Epo requested (2026-09-23) PDF document-load errors center within the available viewer body. src/features/files/PdfView.tsx
- Developer Epo decided (2026-09-23) PDF annotation-load failures use userToast with a Retry action that refetches annotations, replacing the inline read-error banner; show failures independently of PDF download/render completion, including MSW User scenarios. src/features/files/FileViewer.tsx src/features/files/PdfAnnotations.tsx
- Developer Epo approved (2026-09-23) fileError for file/material failures, empty files and Office/text recovery banners, with securityWarning for note edit-permission failures and error for account/collaboration failures; generic displays default to error/Alert02Icon and the warning/AlertCircleIcon mapping is removed. src/components/ui/Icon.tsx src/components/app/ErrorState.tsx src/components/banners/WarningBanner.tsx src/features/files/FileStates.tsx src/features/notes/NoteEditor.tsx
- Developer Epo decided (2026-09-23) Back to workspaces actions on workspace loading, workspace errors and unavailable invitations use navigationBack and ghost-hover. src/components/app/LoadingLarge.tsx src/components/app/WorkspaceError.tsx
- Developer Epo requested (2026-09-23) the legacy/unsupported-file download action match ghost-hover actions, with a left download icon and the extra 4px icon spacing. src/features/files/FileViewer.tsx

- Developer Epo approved (2026-09-23) converting the entire User scenarios panel into grouped one-click journeys: prepare dedicated fixtures, open the real route or product dialog, perform normal edits/submissions and inject failures through mock transports; retain actual application guards and recovery actions, expose Run again/Reset, cancel prior setup, and avoid replaying edits/submissions on reload. Temporary failures recover on the next explicit Save/Retry; permanent failures keep their application behavior. Artificial error-container previews and manual fault configuration leave this panel; product bugs found during migration are reported separately. src/components/dev/MockScenarioPanel.tsx src/mocks/scenarioJourneys.ts src/mocks/scenarioFixtures.ts src/mocks/scenarioDriver.ts src/mocks/collaboration.ts
- Developer Epo approved (2026-09-23) isolated draft storage for explicit MSW recovery scenarios while ordinary MSW sessions remain separate from durable drafts; browser persistence checks exercise the real IndexedDB implementation. src/features/files/sourceDraft.ts
