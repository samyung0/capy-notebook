- Developer Epo decided DOCX, XLSX and PPTX support live collaboration, including full XLSX row, column and sheet restructuring through BetterOffice fork work; simultaneous spreadsheet conflicts resolve automatically and deterministically (duplicate sheet names receive stable suffixes, one overlapping merge survives, at least one sheet remains). src/office-runtime/officeCollaboration.ts collaboration/src/sourceDocuments.ts vendor/betteroffice/crates/betteroffice-xlsx/src/authority/stable.rs
- Developer Epo decided Office saving follows Plate: shared edits persist automatically, Saved requires a durable database checkpoint acknowledgment, Ctrl/Cmd+S flushes that checkpoint path, leaving editing retains shared changes, Undo reverses the user's own edits, and export and indexing carry separate status. src/features/files/useSourceSession.ts src/features/files/useOfficeRuntime.ts collaboration/src/server.ts
- Developer Epo decided a successful Office reparse/re-ingest promotes the exported file as the new collaboration base, preserves every newer saved edit through native rebasing, and clears all editors' Undo/Redo history at the new epoch. Retain one current source, current live dependency bytes needed by saved edits, one current ingest-cache set and reusable captions; release the old full source. Unsaved edits retain the accepted recovery tradeoff. collaboration/src/sourceHandoff.ts server/internal/store/source_refresh.go src/features/files/useSourceSession.ts src/features/files/useOfficeRuntime.ts
- Developer Epo decided native PDF annotations are private visual overlays (text highlights, pen strokes, text, rectangles, ellipses, eraser) excluded from downloads and AI evidence; Epo revised the toolbar decision (2026-09-22, implementation approved): retain View/Edit and replace the cursor-following floating toolbar with a single fixed top PDF toolbar row: plain Page x of x at left without page arrows, drawing actions and Undo/Redo centered, zoom-out/in icons at right, actual Hugeicons at the app stroke, with no extra View-mode instruction line; below lg hide zoom and keep the center tools horizontally scrollable without a scrollbar using Tabs.tsx’s scroll-fade-x; drawing buttons open their tool menus or activate their drawing effect; Eraser uses a large circle in place of the pointer; undo/redo history lives only in the open session’s local state, separate from durable private annotations; material collaboration keeps its existing behavior. src/features/files/PdfView.tsx src/features/files/PdfAnnotations.tsx src/features/files/PdfAnnotationToolbar.tsx src/features/files/usePdfAnnotations.ts src/features/files/pdfAnnotationGeometry.ts server/internal/store/pdf_annotations.go server/migrations/0028_pdf_drawing_annotations.sql
- Developer Epo decided historical chat citations keep their stored snippet and bounding boxes after re-ingestion even when the boxes become inaccurate, with no source-changed notice, and pending edits leave the stored index intact until replacement indexing succeeds. server/internal/store/source_refresh.go src/features/files/FileViewer.tsx
- Developer Epo decided (2026-09-11) viewing an Office file shows the current saved state, never the stale published blob: view mode calls source-session once (view=true omits indexedState and pendingEffects, and state when checkpoint equals indexedCheckpoint) alongside /links, hands the base bytes straight to the viewer WASM when the base is current, otherwise a disposable worker on the runtime origin composes the editor engines to export base plus state and terminates; the view is not live (a reopen or revision change refetches), Leave Edit keeps its iframe export. src/features/files/useOfficeRuntime.ts src/features/files/officeProtocol.ts src/office-runtime/main.tsx src/office-runtime/exportCheckpoint.ts src/office-runtime/exportCheckpoint.worker.ts server/internal/httpapi/huma_source_documents.go
- Developer Epo decided (2026-09-11) the viewer read of source-session (view=true) is lock-free: read authorization through fileRead, one SELECT joining files to source_documents, no row insert, no account-session lock (a suspended owner's shared Office files keep rendering for viewers like PDFs do), state returned only when the checkpoint is ahead of the indexed one, and a file without a source_documents row answers with its blob alone; the editor and collaboration bootstrap keep the locking SourceSession. server/internal/httpapi/huma_source_documents.go server/internal/store/source_documents.go
- Developer Epo decided (2026-09-11) the browser export worker has one focused vitest: a small xlsx fixture plus a one-cell Yrs edit exported through the same loaders re-opens with that cell. src/office-runtime/exportCheckpoint.worker.ts
- Developer Epo decided (2026-09-11) the direct file replacement upload (reserve a presigned PUT against an expected file revision, then complete) is removed with its 'source_replace' upload target, editor-revision guard and the ingest quotaRecovery flag; Office edits reach the blob only through source refresh publication. server/internal/httpapi/huma_sources.go server/internal/store/uploads.go server/migrations/0005_drop_source_replace.sql pipeline/pipeline/ingest/worker.py
- Developer Epo decided (2026-09-25, replacing the 2026-09-13 binary-media decision) PowerPoint collaboration accepts exactly one fork-owned deck schema version and rejects every other (no migrations, backfills, SourceImport passes or two-phase seed), and keeps source media and the parsed package (packageJson) out of Yjs: both are read from the fingerprinted source package, which every open requires, inserted pictures stay binary on their shape, and rebase overlays store changed parts, media included, as buffers. vendor/betteroffice/crates/pptx-edit/src/deck.rs vendor/betteroffice/crates/pptx-edit/src/rebase.rs vendor/betteroffice/crates/pptx-edit/tests/schema_migration.rs
- Developer Epo decided (2026-09-14) citation highlights for Office and Google-native imports are best effort in the native viewer; avoiding persistent preview PDFs takes priority over exact PDF bounding-box fidelity. Native-citation mapping experiments must evaluate this storage-saving design. bench/parsers/scripts/probe_office_pdf.py bench/parsers/scripts/probe_native_citations.ts
- Developer Epo approved (2026-09-14) editable DOCX/XLSX/PPTX exports for Google Docs/Sheets/Slides imports, accepting exported source-byte storage and quota changes. Office parsing retains a PDF-free durable cache of parsed text and metadata, with temporary PDF conversion only as needed, while preserving autosave, collaboration and reparse publication behavior. Existing DOCX layout and shared-formula bugs remain separate fixes. server/internal/integrations/oauth.go parser/app.py pipeline/pipeline/parse/parser_client.py pipeline/pipeline/ingest/worker.py
- Developer Epo decided (2026-09-25) BetterOffice takes upstream by merging a fixed upstream commit into a branch from origin/capy-ci, resolving conflicts with the keep-ours/keep-upstream tables of the upstream handoff; upstream syncs merge instead of rebasing onto capy-ci. artifacts/2026-09-24-betteroffice-upstream-handoff.md artifacts/2026-09-25-betteroffice-handoff-review.md
- Developer Epo decided (2026-09-25) DOCX charts and drawings the parser keeps opaque (w:pict, w:object, mc:AlternateContent) survive editing and publication: the fork seeds them into Yjs as raw-XML embeds and writes them back on export, together with upstream 4bf205b5. vendor/betteroffice/crates/docx-edit/src/seed.rs vendor/betteroffice/crates/docx-parse/src/serializer/run.rs vendor/betteroffice/packages/docx/src/yrs/yrsToDocument.ts
- Developer Epo decided (2026-09-25) PowerPoint takes upstream's comment data model (pptx:comments root, seeded, snapshotted and written on save) with the upstream merge; a PPTX comments UI is a later decision. vendor/betteroffice/crates/pptx-edit/src/comments.rs
- Developer Epo decided (2026-09-25) DOCX and PPTX viewers and editors measure and paint with the fork's bundled metric-compatible fonts (configureDefaultFonts, the same faces registered as FontFaces under the Office family names in the runtime iframe); a font that fails to load shows an explicit error instead of the unwrapped fallback layout. src/office-runtime/DocxEditorHost.tsx src/office-runtime/DocxViewer.worker.ts src/office-runtime/pptxFonts.ts
- Developer Epo decided (2026-09-25) a save that fails inside the Office engine reports failure (drafts kept) instead of being retried by the failed-store runner; the collaboration service queues Office worker calls on the main thread, times each call out after about 2 minutes, and restarts the worker only after a WebAssembly trap or a timeout. collaboration/src/server.ts collaboration/src/officeRuntime.ts
- Developer Epo decided (2026-09-25) shrinking Office storage is the top priority and Office editing state stores only what users changed: XLSX schema 8 keeps topology plus per-cell overrides, unedited cells come from the fingerprinted source, pending effects come from the overrides (no stored XLSX baseline), a publication rebases edits as overrides over the export and fails explicitly when the result does not match, a concurrent clear and set of a source cell resolves by client order, each row or column insert or delete is one effect and formatting applied to a range is one effect; DOCX images reference parts of the fingerprinted source (user-inserted images keep data URLs until the next publication), the run-boundary cache is kept only for paragraphs with tracked formatting changes, note marks, breaks or empty runs, and seeding uses a fixed client id; stored baselines are derived from the published source (kept only after a publication that rebased later edits), a NULL state means the seed of the base until the first edit, contributor markers use a dedicated marker client id, and a refresh copies the captured state only when a save lands during it. Parser-tolerant XLSX binding waits until after the upstream merge. vendor/betteroffice/crates/betteroffice-xlsx/src/authority/stable.rs vendor/betteroffice/crates/docx-edit/src/seed.rs vendor/betteroffice/shared/office-checkpoint.ts collaboration/src/sourceDocuments.ts collaboration/src/contributors.ts artifacts/2026-09-25-office-storage
- Developer Epo decided (2026-09-25) when an Office handoff completes, an open editor is not remounted: it keeps its current view read-only under a persistent "newer version available" banner whose button reloads the whole page, reporting whether its changes were saved; a client left with unsaved changes keeps the recovery path; merging edits made after a publication waits for offline editing. src/features/files/useSourceSession.ts src/features/files/useOfficeRuntime.ts src/features/files/FileStates.tsx todo-office-bench-and-uat-hardening.md
- Developer Epo decided (2026-09-25) a started Office handoff always completes, so busy files publish on the first attempt instead of retrying for hours: an editor reports ready once the server holds all its updates (nothing unsent, no pending input) instead of after its own checkpoint receipt, the server persists the room once, and after the 10 s window the server closes editors that have not answered and publishes anyway, sending them to recovery like a disconnect; the publishing coordinator waits about 60 s for instance acknowledgements; an editor refused authentication because the room is locked for a publication retries after a short delay and shows no error unless that retry also fails, while other authentication failures show as today. collaboration/src/sourceHandoff.ts collaboration/src/server.ts src/features/files/useSourceSession.ts src/features/files/sourceProvider.ts src/features/files/useOfficeRuntime.ts
- Developer Epo accepted (2026-09-25) that after the upstream merge, schema-7 spreadsheets skip upstream's preserved row, column and cell markup on save (markup the model does not track), as capy-ci always did, until the lazy-cell overlay maps it through the live axes; and an array formula keeps its t="array" range only while its anchor cell still holds its original content, otherwise it saves as a single-cell formula. Both stay documented in openwiki/frontend/office-files.md and next to the code. vendor/betteroffice/crates/betteroffice-xlsx/src/workbook.rs
- Developer Epo decided (2026-09-25) store-only Office files publish export-only automatically (the saved edits become the file's bytes, no parser or provider call, no charge) under the Office refresh trigger regardless of workspace auto-reparse, because publishing without parsing costs nothing and auto-reparse keeps meaning automatic parsing; Process stays the owner's opt-in to parsing, charged as the file's first parse. collaboration/src/sourceDocuments.ts server/internal/store/source_documents.go
- Developer Epo decided (2026-09-25) an engine upgrade that changes seed output (golden seed tests decide per format) runs as a maintenance window: Office editing pauses, every file with unpublished edits publishes on the old engine at platform cost, blocked owners included (maintenance republishes skip the credit, storage-quota and owner account-state checks), the deploy waits until all have published, then epochs bump and states drop in one statement so rooms reseed on the new engine; upgrades that keep seeds only bump epochs. The upstream merge lands on a branch first, the storage changes build on it, and one window ships both. Store-only files, trashed files and files of owners pending deletion or suspended publish export-only (the saved edits become the file's bytes, its index is dropped, no parser or provider call), a maintenance republish that fails for good falls back to export-only, and the reset copies every state it drops into an archive kept 30 days. The refresh scheduler reprocesses an export-only file at platform cost, regardless of workspace auto-reparse, once its owner is active and it is out of the trash (store-only files wait for their owner's manual processing); a failed maintenance republish retries daily, except a parse that timed out or ran out of memory, which waits for new bytes or a new parser version. collaboration/src/sourceDocuments.ts server/internal/store/source_refresh.go server/internal/store/trash.go
