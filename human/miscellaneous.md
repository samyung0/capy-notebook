- Developer requested (2026-09-21) Workspaces and Create grids size each row independently to its tallest card rather than equalizing heights across all rows. src/routes/Workspaces.tsx src/routes/Create.tsx
- Developer Epo decided the product is Capy Notebook in every language including Chinese, the registered company is Stable Studio Limited, and product support uses support@stablestudio.org. messages/en.json messages/zh.json
- Developer Epo decided bench is excluded from ruff formatting and lint-fix, including the pre-commit hook, through `extend-exclude` with `force-exclude`. pyproject.toml package.json
- Developer Epo requested (2026-09-13) fixing the failing root `uv run --with ruff ruff check --fix` command. pyproject.toml parser/odl/java.py
- Developer Epo decided BetterOffice uses `capy-ci` as the integration branch supplying Capy Notebook's pinned submodule commits; `main` tracks upstream; feature branches start from and rebase onto `capy-ci`. AGENTS.md .gitmodules
- Developer Epo decided the app nav stays mounted in `AppShell` and routes that render without it declare `staticData.hideSidebar`, which the shell reads off the committed router matches so the nav reshapes in the same render as the outlet swap; opened workspaces are the only such route today, and reading the pending `location` instead cost a second layout shift while the outgoing page waited on the incoming route chunk. src/components/app/AppShell.tsx src/router.tsx
- Developer Epo decided (2026-09-10) sign-in is a Capy-designed landing page at `/sign-in` (brand copy left, auth card right with Google, Microsoft, email plus password, forgot-password and create-account links), `/sign-up` is the same page in sign-up mode with an email-code step and the `clerk-captcha` container, `/forgot-password` is one page with two steps (email, then code plus new password), `/sso-callback` renders Clerk's redirect callback, Clerk's prebuilt pages are gone, `ClerkProvider` carries `signInUrl`/`signUpUrl`, and the same-origin `redirect_url` query keeps the post-auth destination; landing copy is the drafted headline, paragraph and feature pills in paraglide en and zh. src/routes/SignIn.tsx src/routes/ForgotPassword.tsx src/routes/SsoCallback.tsx src/components/app/AuthProvider.tsx src/router.tsx
- Developer Epo decided (2026-09-10) passwords are 12+ characters with at least one digit and one special character, validated client-side on the sign-up and new-password forms only (Clerk instances enforce the 12 minimum; sign-in validates non-empty), and OAuth users are never asked to set a password. src/features/auth/password.ts
- Developer Epo requested (2026-09-15) SPA navigation between auth pages to avoid reloading React; router links preserve the current search parameters. src/features/auth/AuthLanding.tsx src/routes/ForgotPassword.tsx
- Developer Epo decided (2026-09-10) the first-run onboarding dialog opens on the dashboard when the Clerk user lacks `unsafeMetadata.onboardedAt`, uploads photos to Clerk and stores a curated avatar override in Capy, saves the name through `PATCH /api/me`, Confirm and Skip both write `onboardedAt` to Clerk and Skip writes nothing to Capy's database; existing accounts see the dialog once. src/features/auth/OnboardingDialog.tsx src/routes/Dashboard.tsx
- Developer Epo approved (2026-09-14) the transitions-polish motion proposal and preview: shared namespaced tokens, faster popup exits, motion for custom editor popups, targeted entrance blur, notification copy/read-state transitions, initial loading reveals and copy feedback; preserve direct gestures and reduced motion, and defer Sonner's separate removal-lifecycle change. src/styles/tokens/motion.css src/components/ui/PopupMotion.tsx src/components/ui/ContentSwap.tsx src/features/notification/NotificationItem.tsx openwiki/frontend/motion.md
- Developer Epo approved (2026-09-14) keeping Popover, Select and DropdownMenu behavior distinct while sharing motion rules, rebuilding Menu on DropdownMenu, removing its unused baseUIRender hook, and migrating appropriate Popover command lists to DropdownMenu. src/components/ui/Menu.tsx src/features/notes/toolbar/ToolbarMenuRow.tsx
- Developer Epo decided (2026-09-14) to ship the approved static DiceBear catalog with frozen sequential IDs, 24 shuffled Avataaars and the approved Sprouts, Critters, Notionists and Waves; all styles are selectable through a separate icon-picker dialog reused by onboarding and workspace settings, storing only the catalog ID. New and existing workspaces receive a persisted random Waves default and clones retain their icon; workspace colors are removed (revised by Epo, 2026-09-16). public/icons src/components/ui/IconPicker.tsx server/migrations/0017_icon_catalog.sql
- Developer Epo decided (2026-09-14) profile photos remain in Clerk, real OAuth/uploaded photos take initial precedence, and accounts without a real photo receive a persisted Avataaars default. A chosen catalog icon overrides subsequent Clerk syncs; confirming an uploaded photo sends it directly to Clerk and clears the icon override only after upload succeeds. Skip preserves the initial choice. src/features/auth/OnboardingDialog.tsx server/internal/store/users.go
- Developer Epo decided (2026-09-14) Help and Legal contains the existing support cards, FAQs and contact link, replaces the separate Support sidebar entry, and links to a Credits page with compact author rows, source/license links and retained notices for the curated artwork and DiceBear generator. src/routes/HelpAndLegal.tsx src/routes/Credits.tsx
- Developer Epo approved (2026-09-14) the reference 280ms error shake on Input, Textarea and TagSelect once per invalid state, a stationary 280ms InputError fade-in, constant border widths, and reduced-motion support; validation owns error clearing and unchanged invalid states do not replay on submit. src/components/ui/Input.tsx src/components/ui/TextArea.tsx src/components/ui/TagSelect.tsx src/styles/tokens/motion.css
- Developer Epo decided (2026-09-16) waves workspace icons read light at top and dark at bottom, rotation within 180° ± 45° in the DiceBear frame; only the rotate() transform is edited, wave shapes and colors stay. public/icons/waves-*.svg
- Developer Epo decided (2026-09-16) file and material icons in the workspace file tree, Files page, dashboard recent items and Generate tiles are Catppuccin vscode-icons (css-variables flavor, MIT) served as one hashed sprite through FileIcon at stroke 1.3, colors bound to the theme: Catppuccin Latte under data-theme latte, Mocha under mocha, Macchiato under macchiato; the extension refines the backend FileKind on the frontend (code files, video under audio, csv under sheet); materials map note java-enum, quiz stackblitz, flashcards vhs, mindmap forgejo, diagram api-blueprint (note/flashcards/diagram mappings revised by Epo, 2026-09-22); the center content header shows the same glyph and chapter rows use _folder_open/_folder as the expand toggle. src/components/ui/FileIcon.tsx src/lib/fileIcons.ts src/assets/catppuccin.svg src/styles/tokens/catppuccin.css src/features/materials/CenterContentHeader.tsx src/routes/WorkspaceOpen.tsx
- Developer Epo decided (2026-09-16) a third theme macchiato exists for both styles as a verbatim copy of the mocha tokens to be tuned independently, dark, swatch #24273a, label Macchiato, mocha stays the dark default. src/theme/ThemeProvider.tsx src/styles/tokens/themes/notion.css src/styles/tokens/themes/classroom.css src/summary/summary.css
- Developer Epo decided (2026-09-17) Avatar dimensions and initials font size are customized through className to support responsive sizing. src/components/ui/Avatar.tsx
- Developer Epo decided (2026-09-17) FileIcon dimensions are customized through className for responsive sizing, matching Avatar. src/components/ui/FileIcon.tsx
- Developer Epo decided (2026-09-17) Avatar and FileIcon callers use Tailwind spacing utilities for dimensions, such as size-4 instead of size-[16px].
- Developer Epo decided (2026-09-17) the dashboard Recent Items scrollbar sits on the panel's outer edge outside its content padding, and the sidebar column keeps overflow visible so the panel shadow renders fully. src/features/dashboard/RecentItemsCard.tsx src/routes/Dashboard.tsx src/components/app/layout.tsx
- Developer Epo decided (2026-09-17) the Workspaces page places the labeled New workspace action opposite Sort and Filter, using the same ghost-button styling. src/routes/Workspaces.tsx
- Developer requested (2026-09-17) existing icons on the workspace sort dropdown items. src/routes/Workspaces.tsx
- Developer requested (2026-09-17) optional Icon and Description during workspace creation, matching edit validation; share the General form fields and use SimpleDialog for create and standalone edit while preserving embedded settings. src/features/workspace/WorkspaceFormDialog.tsx src/features/workspace/WorkspaceFormCreateDialog.tsx src/features/workspace/WorkspaceFormEditDialog.tsx server/internal/httpapi/apimodel/requests.go
- Developer requested (2026-09-17) a frontend default workspace icon: each new creation draft selects a random Waves catalog icon, keeps it stable while open, and submits the previewed selection. src/features/workspace/WorkspaceFormDialog.tsx
- Developer requested (2026-09-17) extracting the top-bar search dialog into its own file and matching file-tree/recent-item Catppuccin file and material icons. src/components/app/SearchDialog.tsx src/components/app/TopInsetBar.tsx src/lib/fileIcons.ts server/internal/store/queries.go
- Developer requested (2026-09-17) workspace search results show each workspace's saved catalog icon, matching workspace cards; mock and real search responses carry iconId. src/components/app/SearchDialog.tsx src/mocks/handlers.ts server/internal/store/queries.go
- Developer requested (2026-09-17) the 14 audited main-app dialog primary footer/submit buttons explicitly use accent purple; retain danger and secondary button variants and the shared Button dark default. src/components/ui/NameFormDialog.tsx src/components/ui/IconPicker.tsx src/features/workspace/WorkspaceFormDialog.tsx src/features/workspace/AddSourceDialog.tsx src/features/workspace/GenerateFormDialog.tsx src/features/tasks/TaskEditDialog.tsx src/features/schedule/EventFormDialog.tsx src/features/schedule/LabelEditDialog.tsx src/features/flashcards/CardEditModal.tsx src/features/notes/Collaboration.tsx src/features/notes/YouTubeDialog.tsx src/features/notes/blocks/FlashcardsDialog.tsx src/features/notes/blocks/QuizDialog.tsx src/routes/Quizzes.tsx
- Developer requested (2026-09-17) HugeIcons Copy02 for workspace clone actions through a shared clone icon mapping. src/components/ui/Icon.tsx src/components/ui/WorkspaceCard.tsx src/routes/WorkspaceOpen.tsx src/routes/Explore.tsx
- Developer requested (2026-09-17) removing the bottom Clone workspace action from workspace settings. src/features/workspace/WorkspaceSettingsDialog.tsx
- Developer requested (2026-09-17) permanent workspace submit spacing calculated from a shared tag dropdown height minus button height, independent of dropdown visibility; include clearance to keep the dropdown above the buttons. src/styles/tailwind.css src/components/ui/TagSelect.tsx src/components/ui/Button.tsx src/features/workspace/WorkspaceFormDialog.tsx
- Developer requested (2026-09-17) keeping button heights in their existing Tailwind size classes rather than a shared CSS height variable, preserving theme design flexibility. src/components/ui/Button.tsx src/styles/tailwind.css
- Developer requested (2026-09-17) shared textareas grow to five visible lines, then scroll vertically. src/components/ui/TextArea.tsx
- Developer requested (2026-09-18) workspace-card menus align to the trigger's left edge and can extend beyond the card, retaining viewport collision handling. src/components/ui/WorkspaceCard.tsx

- Developer requested (2026-09-18) separating the non-component exports flagged by Vite Fast Refresh into helper modules and extracting router JSX into component-only modules. src/components/app/RouteComponents.tsx src/router.ts src/features/workspace/sourcePercentages.ts src/features/workspace/sourceInspectionGuard.ts src/features/workspace/toolErrorMessage.ts src/features/workspace/contentActionTarget.ts src/components/ui/flattenTagErrors.ts src/components/dev/mockDialogOptions.ts
- Developer Epo approved (2026-09-18) running `pnpm exec tsc -b --noEmit` after lint-staged in pre-commit to catch TypeScript errors without invoking office:prepare, and making InlineIcon.className optional to match Button's optional icon classes. .husky/pre-commit src/components/ui/Button.tsx
- Developer requested (2026-09-18) constant stroke thickness as an opt-in `non-scaling-svg` Tailwind utility applied to every Icon rendered at size-7 (28px) or larger, whether sized by className or the size prop. src/styles/tailwind.css src/features/materials/CenterContent.tsx src/features/workspace/AddSourceDialog.tsx src/features/files/FileViewer.tsx src/routes/QuizAttempt.tsx src/routes/Thinking.tsx src/routes/FlashcardStudy.tsx src/components/banners/DashboardDefaultBanner.tsx src/components/app/ErrorState.tsx
- Developer Epo decided (2026-09-18) the opened workspace page defaults to two columns: the viewer on the left keeps full height and takes the width for landscape files; its header starts with a single-icon two/three column toggle, then a workspace menu on the workspace name (dropdown listing the other workspaces with a fixed Workspace settings button at the bottom, replacing the purple card and the back link), then the open file; one right panel under the unchanged top inset bar shows Files, Chat or Generate, one at a time; from lg up every tab row ends with small icon buttons of reserved width: the active tab's own actions (collapse chapters, chat history, new chat), then a circled plus opening Upload or import, New file (the Add source dialog on its Create tab) and New chapter, and a settings gear; below lg those actions fold into a dots menu next to the plus; the tabs scroll horizontally with a hidden scrollbar and a shadcn-style scroll fade when they overflow; three columns pins the file tree left; chapter rows lead with an expand chevron and use the hugeicons Archive01 glyph; the column toggle is the panel-left icon with plain styling in both states; the workspace menu trigger is Button ghost-hover and the menu footer is only a left-aligned ghost-hover sm Workspace settings button; the curate switch becomes a composer pill labelled Curate mode; the tab switch is the existing underline Tabs component (the stick-out strip is set aside); a pinned Files panel gets a card-title header instead of a tab; icons are the real hugeicons glyphs (Archive01 for chapters, PanelLeft for the toggle). Layouts below xl (two columns at lg, one column with a bottom drawer opened from a floating pill below lg) are drawn and accepted as the final mock round. Epo decided (2026-09-18, after the Opus review) the only way back is a small navigationBack icon button left of the panel toggle (browser back covers the rest); the Add source dialog's Create tab stays a placeholder Epo builds separately; the bottom drawer keeps no drag-up snap point. src/routes/WorkspaceOpen.tsx src/features/workspace/FilesPanel.tsx src/features/workspace/PanelTabRow.tsx src/features/workspace/WorkspaceMenu.tsx src/features/workspace/ChatPanel.tsx src/features/workspace/GeneratePanel.tsx src/features/workspace/AddSourceDialog.tsx src/features/materials/CenterContent.tsx src/features/materials/CenterContentHeader.tsx src/components/ui/Tabs.tsx src/components/ui/Drawer.tsx src/styles/tailwind.css artifacts/2026-09-18-workspace-layout-mocks.html
- Developer requested (2026-09-18) every dialog footer/submit button uses Button size lg, matching ConfirmDialog. src/components/ui/Dialog.tsx src/components/ui/IconPicker.tsx src/features/flashcards/CardEditModal.tsx src/features/notes/Collaboration.tsx src/features/notes/YouTubeDialog.tsx src/features/notes/blocks/FlashcardsDialog.tsx src/features/notes/blocks/QuizDialog.tsx src/features/notes/toolbar/NoteToolbar.tsx src/features/notes/toolbar/ToolbarWidgetSettingsDialog.tsx src/features/schedule/LabelEditDialog.tsx src/features/workspace/GenerateFormDialog.tsx src/routes/Quizzes.tsx
- Developer requested (2026-09-18) workspace sort trigger and dropdown descriptions show the direction; selecting an option keeps the menu open, and selecting the active option reverses its order. src/routes/Workspaces.tsx src/components/ui/Menu.tsx
- Developer Epo requested (2026-09-18) applying and checking thinner default scrollbars locally, using the proposed 6px native CSS scrollbar styling. src/styles/tailwind.css
- Developer requested (2026-09-18) removing the empty viewer's Browse files button, commented JSX and onBrowseFiles prop wiring. src/features/materials/CenterContent.tsx src/routes/WorkspaceOpen.tsx
- Developer requested (2026-09-18) LF line endings for the local Capy Notebook and BetterOffice checkouts; configure both repositories with core.autocrlf=input and core.eol=lf.
- Developer Epo decided (2026-09-18) a Create page at /create replaces the Quizzes and Flashcards list pages (old list URLs redirect; attempt, edit, study, result and share routes stay) and lists every note, quiz and flashcard set the user owns or reaches through workspace membership, standalone or in-workspace, in grid (compact cards) and list views persisted in localStorage src/routes/Create.tsx src/features/materials/MaterialListCard.tsx src/api/hooks.ts, sorted by Updated (default, newest first), Created, Name or Kind with direction toggled on re-select, filtered by kind, location (workspace file, embedded in note, standalone) and workspace, with no tab bar; mindmap and diagram materials stay unchanged and unlisted.
- Developer Epo decided (2026-09-21) Create page items show a bare Catppuccin kind icon; workspace items show only the workspace name beneath the title in grid view (no workspace icon, overwritten 2026-09-22 for Create, Files and Trash alike), and only the workspace name in the list Workspace column; quiz/card counts use their own Details line without progress, and updated time is last. Create cards are smaller and more tightly spaced than Workspaces cards; grid/list controls are compact icon buttons; cards and rows use native router links with existing destinations. List Kind is omitted. Item actions remain unchanged. src/routes/Create.tsx src/features/materials/MaterialListCard.tsx src/components/app/ListToolbar.tsx
- Developer Epo decided (2026-09-18) the Workspaces page sort menu and filter popover become one reusable component shared with the Create page. src/components/app/ListToolbar.tsx src/routes/Workspaces.tsx src/routes/Files.tsx
- Developer Epo decided (2026-09-18) quiz attempts and Review mistakes move to a Learning page at /learning under a "Past results" tab, keeping the existing table design. src/routes/Learning.tsx src/router.ts src/components/app/Sidebar.tsx
- Developer Epo decided (2026-09-19) the Create page and the Files page list only what the user owns (materials and files in workspaces they own plus standalone materials), never member workspaces, through server-side filtering, sorting and keyset pagination on GET /api/materials and GET /api/files (kind, workspace, location for materials, sort, direction, cursor, limit) consumed as infinite queries. server/internal/store/listing.go server/internal/httpapi/huma_materials.go server/internal/httpapi/huma_content.go src/api/hooks.ts
- Developer Epo decided (2026-09-19) after review: inserting a quiz, flashcard set or mermaid block while the caret sits inside a callout, column, table or other container does nothing (no dialog, no block), a pasted reference nested inside a container is lifted to the top level; the old GET /api/quizzes and GET /api/flashcards list routes and their hooks are removed; the top-bar search keeps embedded quizzes and flashcard sets and opens them as the workspace note that embeds them; list pages page 40 items by default.
- Developer Epo approved (2026-09-19) the review fixes: an embedded row is trashed only after a projection has referenced it once (no clock window), a dead worker's lease reaper clears the note's index job, the sidecar parks a note only on a 4xx other than 429, note vector reuse reads the previous content row, note content hashes are prefixed so a note never shares a content row with a file, a pending reference is claimed in the shared document before one client creates its row, the internal note-text endpoint is workspace-scoped, malformed list cursors answer 400, the note scheduler cannot abort the source refresh pass, empty pasted fences get a minimal body, unresolvable references export as a placeholder fence, agent trash/inspect/edit refuse note ids passed as files, and dead hooks and message keys are removed.
- Developer requested (2026-09-20) WorkspaceOpen header's three-dot button matches the left navigation controls through instance styles only; add space before the slightly smaller workspace icon, match the filename typography to the workspace name, enlarge the slash without widening its spacing, and vertically center the header contents. src/routes/WorkspaceOpen.tsx src/features/workspace/WorkspaceMenu.tsx src/features/materials/CenterContentHeader.tsx
- Developer requested (2026-09-20) add app tooltips only to WorkspaceOpen's circled plus and workspace switcher for now; replace the switcher's native title tooltip. src/features/workspace/PanelTabRow.tsx src/features/workspace/WorkspaceMenu.tsx
- Developer requested (2026-09-21) extend WorkspaceOpen's tab-row divider beneath the action buttons while preserving each active tab's underline and the row's horizontal insets. src/features/workspace/PanelTabRow.tsx src/routes/WorkspaceOpen.tsx
- Developer requested (2026-09-21) add a small top gap above the workspace file tree only in two-panel mode; preserve spacing in three-panel and mobile layouts. src/routes/WorkspaceOpen.tsx src/features/workspace/FilesPanel.tsx
- Developer requested (2026-09-21) the Workspaces page defaults to Created, newest first. src/routes/Workspaces.tsx src/router.ts
- Developer requested (2026-09-21) sorting dropdowns and workspace file-tree chapter rows share the `chapter` icon mapping, which uses HugeIcons Archive01. src/components/ui/Icon.tsx src/features/workspace/FilesPanel.tsx src/routes/Create.tsx src/routes/Files.tsx src/routes/Workspaces.tsx
- Developer requested (2026-09-21) Create items updated less than one minute ago display as one minute ago. src/features/materials/MaterialListCard.tsx
- Developer requested (2026-09-21) Learning MSW seeds Review mistakes with the missed and unanswered questions from its imperfect attempt fixtures. src/mocks/db.ts
- Developer requested (2026-09-21) Files and Trash share Create's persisted compact grid/list presentation and card/row styling, with bare Catppuccin icons and no file-kind labels; (2026-09-22) the three pages render through one shared ItemCard/ItemList component (Create's typography, padding and gaps, whole-card link, top-right action menu that Select mode swaps for the toggle), and Files cards carry the workspace file tree's Rename/Properties/Delete menu without Move; ContentActions' delete confirmation describes the trash (30-day restore, storage still charged) instead of an irreversible delete, since files and materials always trash. src/features/workspace/ContentActions.tsx src/components/app/ItemCard.tsx src/routes/Files.tsx src/routes/Create.tsx src/features/materials/MaterialListCard.tsx src/components/app/ListToolbar.tsx
- Developer requested (2026-09-21) Files and Trash have an icon-and-text Select toolbar button; selection mode replaces sort/filter with Select all and selected-item actions: delete files to trash, restore or permanently delete trash items, and empty trash. src/routes/Files.tsx src/components/app/ListToolbar.tsx
- Developer requested (2026-09-21) entering Select mode keeps the single-row toolbar height stable on Files and Trash. src/components/app/ListToolbar.tsx src/routes/Files.tsx
- Developer requested (2026-09-21) selectionActions owns its parent container, replacing the whole sort/filter group so selection styling is independent. src/components/app/ListToolbar.tsx src/features/files/FileSelectionActions.tsx
- Developer requested (2026-09-21) Files and Trash use one Select all button that becomes Clear selection with an icon when every item across all pages is selected, replacing the separate Clear selection button. src/features/files/FileSelectionActions.tsx src/routes/Files.tsx
- Developer requested (2026-09-21) the selected-item Delete action sits immediately left of Cancel, matching Cancel's size and ghost-hover styling while retaining its red destructive text and icon. src/routes/Files.tsx src/features/files/FileSelectionActions.tsx
- Developer requested (2026-09-21) MSW starts with enough Create and Trash fixtures to exercise pagination, with paginated trash responses. src/mocks/db.ts src/mocks/handlers.ts
- Developer Epo selected (2026-09-22) document-page mock A: preserve the regular app sidebar, reuse the workspace document frame for items opened from Create/Files on dedicated pages defaulting to View, hide workspace actions, and show only the existing navigationBack icon before the document kind icon/title, removing the Create/Files label, panel toggle and slash; Epo approved implementation and an Astra high review after the final mock (2026-09-22); (2026-09-22) the document page hides the top inset bar from lg up, so the document frame starts at the top of the page and its back button is the way out, and keeps the bar below lg where it carries the only navigation; WorkspaceOpen keeps its inset bar at every width. artifacts/2026-09-22-document-page-mocks.html src/routes/MaterialOpen.tsx src/routes/Create.tsx src/routes/Files.tsx src/features/materials/CenterContent.tsx src/features/materials/CenterContentHeader.tsx src/features/files/FileModeControl.tsx
- Developer requested (2026-09-22) source editing works under MSW without a collaboration server: the mock room layer seeds text-source rooms from the fixture link, answers source-session and collaboration-token, and a checkpoint writes the text back to the mock link; Office kinds stay 503 until fixtures exist. A `collab-chaos` user scenario ports the chaos peers script in-page (3 peers per open room, cursors, random snippets, join/leave cycles, registered as workspace editors) and follows whichever rooms are open; the sidecar script stays for the real stack; `y-protocols` becomes a dev dependency. After the Opus review Epo decided (2026-09-22): under MSW the source editor never connects a real provider (missing mock registration throws); relayed updates carry the receiving provider as Yjs origin like Hocuspocus; mock source rooms use the sidecar's `source:<id>:epoch:<n>` name; a room is dropped with its last participant; awareness teardown mirrors Hocuspocus (others forget the leaver, the leaver forgets the others); peers build on the app's MaterialKit plugins, skip void blocks and send cursor positions by hand; member changes invalidate the members query; checkpoint receipts arrive on a microtask; mock checkpoints do not bump file revisions, with draft recovery reserved for explicit isolated scenarios approved on 2026-09-23. The Workspaces sort spec follows the Created default. src/mocks/collaboration.ts src/mocks/chaosPeers.ts src/mocks/handlers.ts src/mocks/scenarios.ts src/features/files/sourceProvider.ts src/features/files/useSourceSession.ts src/components/dev/MockScenarioPanel.tsx src/mocks/scenarios.test.ts e2e/editor/frontend-workspaces.spec.ts
- Developer Epo approved (2026-09-22) the follow-up collaboration mock fixes: persist pending edits before releasing a room, retain encoded checkpoints across room recreation, save chaos peer edits, and retire peer pools when their room disappears or is replaced. Ordinary MSW source sessions keep changes in the mock database and isolate themselves from durable browser drafts; explicit recovery scenarios use the isolated storage approved on 2026-09-23. src/mocks/collaboration.ts src/mocks/chaosPeers.ts src/features/files/sourceDraft.ts
- Developer Epo decided (2026-09-22) the viewer header's action menu (3 vertical dots) renders at full foreground strength with larger horizontal padding than it previously carried. src/features/materials/CenterContentHeader.tsx
- Developer Epo decided (2026-09-22, after the before/after mocks at artifacts/2026-09-22-header-alignment-mocks.html) every icon paired with text sits 1px above the flex centre so it lands on the 14px Fustat cap band, and the text stays put: Select adopts that convention like Button and Menu, lifting its item icon and trigger chevron instead of dropping its label, after Epo trialled it header-local and approved it app-wide. src/components/ui/Select.tsx src/features/materials/CenterContentHeader.tsx src/routes/WorkspaceOpen.tsx src/routes/MaterialOpen.tsx src/components/ui/IconButton.tsx
- Developer Epo decided (2026-09-22) the opened viewer header drops the slash between workspace and document, gives the workspace menu trigger a grey chip background for hierarchy, renders the workspace and document/material icons at one size 20px, and spaces the mode select evenly at 8px between its icon, its label, its chevron and the action menu, the trailing 8px coming from the action button's own left padding so that button keeps its own. The chip uses the Button `gray` variant Epo added; dark themes beyond classroom latte are unfinished and their missing surface tokens are accepted for now. src/features/materials/CenterContentHeader.tsx src/features/workspace/WorkspaceMenu.tsx src/components/ui/Button.tsx
- Developer Epo decided (2026-09-22) the opened viewer header keeps one 8px gap between any icon and the text beside it, so the document icon matches the workspace chip rather than the looser value it had; the workspace menu trigger's chevron rotates on open like the select trigger's; and the left gutter, the lead icon group's right margin and the right gutter all shrink below lg. src/features/materials/CenterContentHeader.tsx src/routes/WorkspaceOpen.tsx src/features/workspace/WorkspaceMenu.tsx
- Developer Epo decided (2026-09-22) below lg the opened workspace is one column: the header drops the workspace menu, and the floating bar loses the gaps between its buttons and gains a workspace settings button so settings stay reachable. Below md the document mode select shows its icon and chevron without the Edit or View word. src/routes/WorkspaceOpen.tsx src/features/materials/CenterContentHeader.tsx
- Developer Epo decided (2026-09-22) workspace settings gains an Others tab holding the clone action and a Danger tab whose label and active underline are the error colour and whose contents sit inside an error-bordered group, currently the delete action; Workspace statistics becomes Statistics. Clone was previously reachable only from the workspace card menu. src/features/workspace/WorkspaceSettingsDialog.tsx src/components/ui/Tabs.tsx
- Developer Epo decided (2026-09-22) the opened workspace's floating bar labels every button including settings, and drops to icons only below sm; the drawer's file tree carries the docked right column's top padding. src/routes/WorkspaceOpen.tsx src/features/workspace/FilesPanel.tsx
- Developer Epo decided (2026-09-22) the workspace Generate panel is presented as Create: its tab reuses the sidebar's Create label in both the floating bar and the right column, and the sidebar's Create entry takes the sparkles glyph, so all three read sparkles plus Create. The generate form's submit button keeps the word Generate under the renamed key action_generate. src/routes/WorkspaceOpen.tsx src/components/app/Sidebar.tsx src/features/workspace/GenerateFormDialog.tsx messages/en.json messages/zh.json
- Developer Epo decided (2026-09-22) the Others and Danger rows carry the Sharing tab's styling: an InputTitle heading over a t-meta hint, and a full-size rounded-input button, outline for clone and the danger variant for delete; the Danger group keeps its own padding because its error border already does the grouping. src/features/workspace/WorkspaceSettingsDialog.tsx
- Developer Epo decided (2026-09-22) workspace deletion keeps its cascade and only its wording changes: both confirm dialogs, on the workspace card and in the Danger tab, title "Delete {name} permanently?", state that files, notes, quizzes and flashcards go with it including items already in its workspace trash and that none of it moves to the trash or can be restored, and label the confirm button Delete permanently. src/components/ui/WorkspaceCard.tsx src/features/workspace/WorkspaceSettingsDialog.tsx messages/en.json messages/zh.json
- Developer Epo decided (2026-09-23) the hover action menu's backdrop reuses Tabs' scroll-fade mask with a fixed 8px left edge and removes the radialBackground option and its custom button padding/offsets (revised 2026-09-23); the workspace file tree carries a vertical scroll fade at both its top and bottom edges, increased from 28px to 36px at Epo's request (2026-09-23); the tree omits the Others heading when the workspace has no chapters, since everything is unfiled and the word names nothing. src/components/ui/HoverActions.tsx src/features/workspace/FilesPanel.tsx src/styles/tailwind.css
- Developer Epo decided (2026-09-23) the dashboard's Recent Files rows carry the same hover action menu as the file tree, without Move because the list spans workspaces; mergeRecentItems carries the source file and material list item so the row can build a ContentActionTarget without refetching. The card waits for its file, material and workspace lists before it paints, trading a longer skeleton for a list that does not rewrite itself once one list lands. src/features/dashboard/RecentItemsCard.tsx src/features/dashboard/recentItems.ts
- Developer Epo decided (2026-09-23) route loaders only prime the query cache and never hold rendering: `page()` discards whatever its loader returns, and the auth-shell route prefetches /me so every page starts it in the first wave. src/router.ts
- Developer Epo decided (2026-09-23) opening a workspace file prefetches it in the route loader, PDF annotations are read alongside the presigned link rather than after the document downloads, and the PDF never waits on its annotations since they are only an overlay. src/router.ts src/api/hooks.ts src/features/files/FileViewer.tsx src/features/files/usePdfAnnotations.ts
- Developer Epo decided (2026-09-23) the heavy-document gate must hold: nothing fetches a material's body before the reader chooses, so the workspace viewer header reads title and kind from the material list and capabilities from the workspace, and reads the body only on the standalone page or for a material the list lacks. src/features/materials/CenterContentHeader.tsx
- Developer Epo decided (2026-09-23) the notification bell loads its list on first open, with the badge driven by the unread count alone; and the account lifecycle rides on GET /api/me as `account`, replacing GET /api/account/status. src/features/notification/NotificationBell.tsx src/api/hooks.ts server/internal/httpapi/apimodel/apimodel.go server/internal/httpapi/huma_account.go server/internal/httpapi/huma_account_lifecycle.go src/components/app/AccountStatusBanner.tsx src/mocks/handlers.ts src/mocks/scenarios.ts
- Developer Epo decided (2026-09-23) the dashboard's Recent Files lists files and top-level materials of every kind from workspaces the user owns or is an explicit member of (visited link/public workspaces excluded) plus their standalone materials, mind maps and diagrams included, embedded quizzes and flashcards left out (GET /api/materials accepts every kind while its no-kind default stays note, quiz, flashcards for Create), via `scope=member` on GET /api/files and GET /api/materials (default `owned` keeps Files and Create unchanged) fetched in parallel and merged client-side, replacing the per-workspace material fan-out; a member workspace whose owner is deleted or deletion-pending drops out, as in the workspace list. server/internal/store/listing.go server/internal/httpapi/huma_content.go server/internal/httpapi/huma_materials.go src/api/hooks.ts src/router.ts src/features/dashboard/RecentItemsCard.tsx src/features/dashboard/recentItems.ts
- Developer Epo decided (2026-09-23) the PDF viewer keeps pdf.js's full-file download with byte-range loading off; the source-file limit (10 MiB Free, 30 MiB Pro) bounds the bytes held in memory, and a download that starts as the presigned link is minted finishes inside its five-minute life. src/features/files/PdfView.tsx

- Developer Epo requested (2026-09-23) restoring workspace file-tree drag and drop: chapters reorder among chapters; files and materials move into/out of chapters and reorder together within a chapter; chapter drop areas use purple tint, insertion lines use solid purple accent, and dropping content outside chapters onto the outer tree unfiles it with a solid purple parent outline. src/features/workspace/FilesPanel.tsx

- Developer Epo requested (2026-09-23) moving ThemeProvider non-component exports into a separate module so Vite Fast Refresh accepts the provider boundary. src/theme/ThemeProvider.tsx src/theme/theme.ts

- Developer Epo requested (2026-09-23) equivalent file-tree insertion targets after one row and before its next sibling share the same line position for chapters and mixed file/material rows. src/features/workspace/FilesPanel.tsx

- Developer Epo requested (2026-09-23) optimistic chapter reordering in the workspace tree, matching file/material moves: update cached order immediately, roll back on failure, and refetch after settlement. src/api/hooks.ts

- Developer Epo requested (2026-09-23) suppressing grey row backgrounds and the entire action-menu wrapper, including its trigger and overflowing fade, during workspace chapter/file/material drags while retaining purple drop indicators; restore normal hover treatment when dragging ends. src/features/workspace/FilesPanel.tsx src/features/files/FileListItem.tsx src/features/materials/MaterialListItem.tsx src/components/ui/HoverActions.tsx

- Developer Epo requested (2026-09-23) 5px horizontal/vertical padding for workspace Files/Chat header actions with zero inter-button gap; viewer back/panel-toggle buttons use more horizontal and less vertical padding with reduced gap, preserving the header layout; Search and Notifications retain their sizes and padding. src/features/workspace/PanelTabRow.tsx src/routes/WorkspaceOpen.tsx
- Developer Epo requested (2026-09-23) matching file/material row padding inside the hover background while preserving chapter indentation. src/features/files/FileListItem.tsx src/features/materials/MaterialListItem.tsx
- Developer Epo requested (2026-09-23) a fixed floating plus button at the Files panel's bottom-right, with a shadow and transitions.dev dropdown-menu-morph motion; it shares the header plus menu's actions. Epo clarified (2026-09-23) to replicate the reference implementation directly, without transitions-polish: a persistent size-transitioned surface with 350ms opening overshoot, 250ms collapse, a sliding/rotating plus cross-fade, and a 5px closing nudge settling over 700ms. src/features/workspace/FilesPanel.tsx src/features/workspace/addSourceMenuItems.ts src/components/ui/Menu.tsx src/styles/tokens/motion.css
- Developer Epo requested (2026-09-23) outside clicks dismiss the floating Add menu without activating underlying files, both floating workspace controls use lighter shadows, and chapterless file trees omit the empty chapter group's spacing. src/components/ui/Menu.tsx src/features/workspace/FilesPanel.tsx src/routes/WorkspaceOpen.tsx
- Developer Epo clarified (2026-09-23) the Files panel's bottom padding is trailing scroll space, visible only at the end; the vertical fade stays at the viewport edge without a permanently blank inset and clears as the last row approaches, before the trailing padding appears. src/features/workspace/FilesPanel.tsx src/styles/tailwind.css

- Developer Epo requested (2026-09-23) removing New note from every workspace Files panel header and removing the duplicate Workspace settings button from the pinned left Files panel in three-column layout; settings stays on the right panel. src/features/workspace/FilesPanel.tsx src/routes/WorkspaceOpen.tsx
