- Developer Epo decided large completed implementations use repeated independent subagent review rounds whose reports live in /private/tmp; reviewers are subagents and obeys the `human` skill
- Developer Epo decided (2026-09-05) that while no production database or data exists, schema changes edit `server/migrations/0001_init.sql` in place instead of adding migration files. server/migrations/0001_init.sql
- Developer Epo decided (2026-09-05) to rename Evo Notes to Capy Notebook across project code and infrastructure, use Capy Notebook unchanged as the Chinese product name, and rename the EVO_* environment prefix including EVO_PRIVATE_BIND_ADDRESS to CAPY_*.
- Developer Epo decided (2026-09-05) the product is Capy Notebook, the registered company is Stable Studio Limited, and product support uses support@stablestudio.org.
- Developer Epo selected (2026-09-06) workspace Settings layout C with the existing Tabs component as in mock A and an extended existing ProgressBar; the button beside Add source opens General, Sharing and Indexing; General reuses workspace-card name/description/color/tags editing, Sharing reuses existing controls, and Indexing shows indexed/unindexed/nonindexable percentages totaling 100%, exactly two pending reparse/reindex count rows without filenames and both automatic-update switches. src/features/workspace/WorkspaceSettingsDialog.tsx src/components/ui/ProgressBar.tsx server/internal/store/queries.go
- Developer Epo authorized (2026-09-06) implementing the agreed Office/source editing design in the current checkout, with Astra high implementation workers and thorough Astra high review of workflows and lifecycles; schema changes go directly into 0001_init.sql, including destructive changes, without a compatibility migration. artifacts/office-editing/plan.md server/migrations/0001_init.sql
- Developer Epo requested (2026-09-06) excluding scripts/grading_benchmark from the Python pre-commit formatting and lint-fix commands. pyproject.toml package.json
