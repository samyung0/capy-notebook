- Developer Epo decided monthly AI credit limits are 1,000 for Free and 20,000 for Pro. server/migrations/0001_init.sql server/internal/store/credits.go pipeline/pipeline/store/db.py src/features/billing/planLimits.ts
- Developer Epo decided model:certify exits when the provider rejects the API key on the model-list request and renders its catalog left-aligned with a check on the selected slug. pipeline/pipeline/model_replay_cert.py pipeline/scripts/certify_agentic_loop_model.py
- Developer Epo decided the model catalog assigns models to slots (a named place the product calls a model, holding one default pin and, for chat/generate/editor/quiz, a per-user override): chat, generate, editor, quiz, ingest, retrieval (ingest indexing and chat/generate query embedding) and captioning (ingest figure captions and standalone image uploads); metering keeps `surface` in usage_events, credit_reservations and provider_sessions because it records the product feature billed, which for retrieval and captioning is chat/generate/ingest. server/internal/models/slot.go server/migrations/0001_init.sql pipeline/pipeline/generated/slot.py
- Developer Epo decided each catalog row carries operator-set `capabilities` (vision, pdf, embedding) with provider `modes` gone from elitellm_providers.json, and one Go map lists what a slot requires: chat needs agentic_loop (derived from agentic_loop_certs.json, never operator-set), retrieval needs embedding, captioning needs vision, generate/editor/quiz/ingest accept any text-responding model with no per-provider allowlist; assignment validation is a subset check, and a registry draft omitting thinking_levels is stored as an empty array and rejected by validation, never NULL. server/internal/models/slot.go server/internal/ops/registry.go
- Developer Epo decided the supported provider set is anthropic, openai, deepseek, zai and deepinfra; Gemini is removed from the catalog, the EliteLLM adapter, the agentic-loop and streaming paths, the cache-shape list and the deployment env, and reinstating it means a fresh adapter rather than a revert. pipeline/pipeline/elitellm/client.py pipeline/pipeline/elitellm/providers.json server/internal/models/elitellm_providers.json server/internal/models/catalog.go pipeline/pipeline/retrieval/usage_extract.py deploy/.env.example
- Developer Epo decided glm-5.3-flash keeps catalog slug `zai` for provider attribution while EliteLLM routes the call to DeepInfra as `zai-org/GLM-5.3-Flash` billed on `DEEPINFRA_API_KEY` (ZAI's own API needs a monthly plan); the hop is billing-only and exact-match, matching `deepinfra/Qwen/Qwen3-Embedding-4B`, not a general router path. pipeline/pipeline/retrieval/usage_extract.py pipeline/pipeline/elitellm/providers.json server/internal/models/elitellm_providers.json
- Developer Epo decided the ops queue summary counts `import` jobs alongside parse and ingest, including expired-lease and oldest-queued aggregates. server/internal/ops/read_store.go
- Developer Epo decided the Sentry environment tag reads `SENTRY_ENVIRONMENT` and falls back to `APP_ENV`, so UAT reports `uat` while its `APP_ENV=production` keeps exercising production safety checks. server/cmd/api/main.go server/cmd/ops/main.go pipeline/pipeline/obs.py collaboration/src/observability.ts deploy/docker-compose.prod.yml deploy/docker-compose.ingest-host.nonprod.yml
- Developer Epo decided the SPA build generates hidden source maps, uploads them to Sentry and deletes them before the bundle ships, gated on `SENTRY_AUTH_TOKEN` so only CI pays the cost. vite.config.ts .github/workflows/deploy-environment.yml
- Developer Epo decided a busy provider answer (429, 503, 529) honors Retry-After and every provider call has a wall budget and an attempt cap, each attempt on a new call id: interactive calls get 2 attempts within 3 seconds and fail closed with a retryable `provider_busy` error carrying Retry-After (generate and quiz grade carry it in the body only) when Retry-After exceeds the budget or the budget ends; ingest embedding, summary and caption calls get 4 attempts within 2 minutes, figure captions retrying any failure and then carrying on empty, while embedding, summary and standalone-image caption calls re-pend the job without spending an attempt with `not_before` from the provider's Retry-After or 30 seconds doubling (never the synthesized client hint), at most 5 busy re-pends per job counted in `jobs.provider_waits`, after which the job fails and the file reports `provider_busy`. pipeline/pipeline/retrieval/models.py pipeline/pipeline/jobs.py pipeline/pipeline/ingest/worker.py pipeline/pipeline/store/db.py server/internal/httpapi/models.go src/lib/errors.ts
- Developer Epo decided the interactive provider timeout is a 15-second idle bound reset on every provider data event, applied per read so a consumer may await between chunks, with a 10-minute whole-stream backstop from which the interactive receipt window derives (backstop plus 5 minutes); interactive completions including generate, live compaction and checkpoint run over the streaming transport so the idle timer applies, non-streaming interactive calls such as embeddings are capped at 15 seconds whole-call, and ingest timeouts are unchanged. pipeline/pipeline/elitellm/client.py pipeline/pipeline/config.py pipeline/pipeline/retrieval/models.py pipeline/pipeline/retrieval/accounting.py
- Developer Epo decided the provider transport parses Retry-After, marks busy answers on the provider error and shares one keep-alive httpx client per event loop; a request cancelled during admission is undone by a detached done-callback that abandons the never-sent call row and frees its lease; the ops usage explorer shows attempts, abandoned and busy counts per provider and model from `provider_calls`, health shows attempts abandoned on a provider 429/503/529 per provider and model over the last hour (a gate refusal writes no row), and per-turn tool-call telemetry stays unpersisted. pipeline/pipeline/elitellm/client.py pipeline/pipeline/retrieval/accounting.py server/internal/ops/read_store.go server/internal/ops/types.go ops/src/pages/costs.tsx ops/src/pages/health.tsx
- Developer Epo decided the workspace owner funds automatic source reparse/reindex batches combining collaborators' edits; exhausted ingest credits pause automatic refresh while durable editing saves continue. server/internal/store/source_documents.go
- Developer Epo approved the SSE review fix so the shared broker accepts subscriptions after Redis recovers from an initial connection failure. server/internal/httpapi/fanout.go
- Developer Epo decided (2026-09-09) model capacity lives in environment-local database rows keyed by transport provider/model, shared across catalog versions and edited through the Ops model form with required concurrency_total > 0 and 0 <= interactive_reserve < total; platform calls read current limits in each admission transaction, missing capacity fails explicitly, interactive uses total and ingest total minus reserve, BYOK bypasses the gate, and existing calls finish after limits are lowered; retain atomic provider_calls/provider_capacity_leases admission and remove CAPY_MODEL_CONCURRENCY and its startup requirement. server/internal/ops/registry.go server/migrations/0001_init.sql pipeline/pipeline/retrieval/accounting.py ops/src/draft-fields.tsx
- Developer Epo decided (2026-09-11) an ingest job admitted at upload reservation and at its first claim runs to completion: later stage rechecks and the parse-to-ingest continuation cover lifecycle and storage only, and the next interactive request refuses an actor past the limit. pipeline/pipeline/ingest/worker.py pipeline/pipeline/store/db.py
- Developer Epo decided (2026-09-11) upload reservation refuses when the actor's used plus reserved plus the browser-computed credit estimate exceeds 1.3× the plan limit, trusting the client estimate because the parser receipt bills measured pages. server/internal/store/credits.go server/internal/httpapi/huma_sources.go server/internal/httpapi/huma_source_upload.go src/api/hooks.ts src/features/workspace/AddSourceDialog.tsx
- Developer Epo decided (2026-09-11) parse measurements accumulate in one mutable per-attempt accumulator shared across thread context copies, so worker-thread parser calls reach the coroutine's billing transaction. pipeline/pipeline/obs.py
- Developer Epo decided (2026-09-11) the Ops registry marks each enabled platform model missing a required runtime config (capacity row, provider credential) with a warning icon naming what is missing. ops/src/registry-domain.ts ops/src/pages/registry.tsx
- Developer Epo decided (2026-09-11) the parse usage_events idempotency key is job-scoped (`parse-receipt:{fingerprint}:{job_id}`) so two jobs that each really ran the parser are each billed while a lost response for one job still recovers without a second charge. pipeline/pipeline/ingest/worker.py
- Developer Epo decided (2026-09-11) a job's first claim means attempts == 1, provider_waits == 0 and no parseJobId; a busy-provider or capacity re-pend keeps its admission. pipeline/pipeline/ingest/worker.py
- Developer Epo decided (2026-09-11) provider import reservations stay on the plain credit exhaustion check without the 1.3× estimate headroom. server/internal/httpapi/huma_sources.go
- Developer Epo decided (2026-09-11) the upload dialog keeps submit disabled while any fast-parse source analysis is running or has failed; the user removes a failed source to proceed, so every reserved fast-parse source carries an estimate. src/features/workspace/sourceDetails.ts src/features/workspace/AddSourceDialog.tsx
- Developer Epo decided (2026-09-11) recording parse usage outside a started attempt raises instead of creating a thread-local accumulator. pipeline/pipeline/obs.py
- Developer Epo decided (2026-09-11) the Ops least-privilege probe skips a forbidden column that no longer exists, so a migration dropping a named column never blocks an older Ops build from booting. server/internal/ops/privileges.go
- Developer Epo decided (2026-09-11) the browser reads the fast-parse extension list from the upload policy (parseModes) as the single source of truth instead of keeping its own copy. src/features/workspace/sourceAnalysis.ts src/features/workspace/sourceDetails.ts
- Developer Epo decided (2026-09-11) one always-open `/api/stream` SSE per signed-in tab carries notifications, ingest progress and coarse workspace tree change events (files, materials) for the workspace named in the query, LLM streams stay separate; the shared broker fans out `notif:*`, `ingest:*` and `tree:*` with the per-user cap raised to 7 streams; every list-visible write to `files` or `materials` from any writer raises the tree event through Postgres triggers and one `LISTEN` per gateway process, and the browser refetches the list, no per-row payload; the stream re-checks account and workspace access at its 25 s ping rather than the LLM streams' 5 s live context, and a workspace stream is refused while the gateway's tree LISTEN is detached. server/internal/httpapi/sse_events.go server/internal/httpapi/fanout.go server/internal/store/tree_events.go server/migrations/0004_workspace_tree_notify.sql
