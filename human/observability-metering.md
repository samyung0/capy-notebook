- Developer Epo decided monthly AI credit limits are 1,000 for Free and 20,000 for Pro. server/migrations/0001_init.sql server/internal/store/credits.go pipeline/pipeline/store/db.py src/features/billing/planLimits.ts
- Developer Epo decided model:certify exits when the provider rejects the API key on the model-list request. pipeline/pipeline/model_replay_cert.py pipeline/scripts/certify_agentic_loop_model.py
- Developer Epo decided the model:certify catalog is left-aligned with a check on the selected slug. pipeline/scripts/certify_agentic_loop_model.py
- Developer Epo decided (2026-09-04) the model catalog concept formerly called surface is a slot: a named place the product calls a model, holding one default pin and, for chat/generate/editor/quiz, a per-user override; slots are chat, generate, editor, quiz, ingest, retrieval (was embedding; used by ingest indexing and chat/generate query embedding) and captioning (was vision; used by ingest figure captions and standalone image uploads). Metering keeps `surface` (usage_events, credit_reservations, provider_sessions) because it records the product feature billed, which for retrieval and captioning is chat/generate/ingest. server/internal/models/slot.go server/migrations/0001_init.sql pipeline/pipeline/generated/slot.py
- Developer Epo decided (2026-09-04) each catalog row carries operator-set `capabilities` (vision, pdf, embedding) replacing `params.modalities`, provider `modes` are removed from elitellm_providers.json, and one Go map lists the capabilities a slot requires: chat needs agentic_loop (derived from agentic_loop_certs.json, never operator-set), retrieval needs embedding, captioning needs vision, other slots need nothing; assignment validation is a subset check. server/internal/models/slot.go server/internal/ops/registry.go
- Developer Epo decided glm-5.3-flash keeps catalog slug `zai` for provider attribution but EliteLLM routes the call to DeepInfra as `zai-org/GLM-5.3-Flash`, billed on `DEEPINFRA_API_KEY`, because ZAI's own API requires a monthly plan while DeepInfra is pay-as-you-go; the hop is billing-only, exact-match only, not a general router path, matching the same treatment for `deepinfra/Qwen/Qwen3-Embedding-4B`. pipeline/pipeline/retrieval/usage_extract.py pipeline/pipeline/elitellm/providers.json server/internal/models/elitellm_providers.json
- Developer Epo decided (2026-09-04) slot assignability widens naturally with model capability: the baseline slots generate, editor, quiz and ingest accept any model that responds in text, and a model earns further slots by carrying the capabilities those slots require; no per-provider allowlist gates the baseline slots. server/internal/models/slot.go
- Developer Epo decided (2026-09-04) a registry draft that omits thinking_levels is stored as an empty array and rejected by validation, never written as NULL. server/internal/ops/registry.go
- Developer Epo decided (2026-09-04) the supported provider set is anthropic, openai, deepseek, zai and deepinfra: Gemini leaves the catalog, the EliteLLM adapter, the agentic-loop and streaming paths, the proven inclusive cache-shape list and the deployment env because its cost against performance loses to the seeded rows and its adapter was defective; reinstating Gemini means a fresh adapter, not a revert. pipeline/pipeline/elitellm/client.py pipeline/pipeline/elitellm/providers.json server/internal/models/elitellm_providers.json server/internal/models/catalog.go pipeline/pipeline/retrieval/usage_extract.py deploy/docker-compose.yml deploy/.env.example
- Developer Epo decided (2026-09-05) the ops queue summary counts `import` jobs alongside parse and ingest, including their expired-lease and oldest-queued aggregates. server/internal/ops/read_store.go
- Developer Epo decided (2026-09-05) the Sentry environment tag reads `SENTRY_ENVIRONMENT` and falls back to `APP_ENV`, so UAT reports `uat` while its `APP_ENV=production` keeps exercising production safety checks; before this, UAT and production shared one bucket. server/cmd/api/main.go server/cmd/ops/main.go pipeline/pipeline/obs.py collaboration/src/observability.ts deploy/docker-compose.prod.yml deploy/docker-compose.ingest-host.nonprod.yml
- Developer Epo decided (2026-09-05) the SPA build generates hidden source maps, uploads them to Sentry, and deletes them before the bundle ships, gated on `SENTRY_AUTH_TOKEN` being present so only CI pays the cost. vite.config.ts .github/workflows/deploy-environment.yml
- Developer Epo decided (2026-09-05) provider admission is keyed by transport provider plus model for every provider so each model's outbound traffic has its own internal cap even where the provider limits per account; capacity comes from `CAPY_MODEL_CONCURRENCY` entries `provider:model=total/interactive_reserve`, an absent entry leaves that model ungated, interactive callers may use the whole total while ingest callers use total minus the reserve, and only platform-key calls take a lease. Production values are deepinfra zai-org/GLM-5.3-Flash 200/120 and deepinfra Qwen/Qwen3-Embedding-4B 200/80 (DeepInfra documents 200 concurrent per model per account); UAT has its own account and numbers, local UI proxied to UAT is UAT, full local compose runs on developer keys. pipeline/pipeline/config.py pipeline/pipeline/retrieval/accounting.py deploy/.env.example deploy/env-manifest.json
- Developer Epo decided (2026-09-05) the gate lives in the Postgres `provider_capacity_leases` table both VMs already share: the lease is taken in the same transaction as the pre-call `provider_calls` row with the call id as lease id and the receipt deadline as expiry, released when the call ends, and waiters poll with jitter within the call's budget. pipeline/pipeline/retrieval/accounting.py pipeline/pipeline/store/db.py pipeline/pipeline/retrieval/models.py
- Developer Epo decided (2026-09-05) a busy provider answer (429, 503, 529) honors Retry-After and every provider call has both a wall budget and a maximum attempt count, each attempt on a new call id: interactive calls get 2 attempts within 3 seconds and fail closed with a retryable `provider_busy` error carrying Retry-After when Retry-After exceeds the budget or the budget ends; ingest embedding, summary and caption calls get 4 attempts within 2 minutes, after which figure captions stay best effort and carry on empty (amended 2026-09-06: standalone-image captions, being the whole content, re-pend with embedding and summary), while embedding, summary and standalone-image caption calls re-pend the job without spending an attempt with `not_before` from Retry-After or 30 seconds doubling, at most 5 busy re-pends per job counted in `jobs.provider_waits`, after which the job fails and the file reports `provider_busy`. pipeline/pipeline/retrieval/models.py pipeline/pipeline/jobs.py pipeline/pipeline/ingest/worker.py pipeline/pipeline/store/db.py server/migrations/0001_init.sql pipeline/pipeline/retrieve/service.py pipeline/pipeline/retrieval/agent.py server/internal/httpapi/models.go server/internal/httpapi/server.go src/lib/errors.ts
- Developer Epo decided (2026-09-05) the interactive provider timeout is a 15-second idle timeout that resets on every provider data event with a 10-minute whole-stream backstop from which the interactive receipt window derives (backstop plus 5 minutes); interactive completions including generate, live compaction and checkpoint run over the streaming transport and are assembled internally so the idle timer applies to them, non-streaming interactive calls such as embeddings are capped at 15 seconds whole-call, and ingest timeouts are unchanged. pipeline/pipeline/elitellm/client.py pipeline/pipeline/config.py pipeline/pipeline/retrieval/models.py pipeline/pipeline/retrieval/accounting.py
- Developer Epo decided (2026-09-05) the provider transport parses Retry-After, marks busy answers on the provider error and shares one keep-alive httpx client per process (one per event loop); the ops usage explorer gains attempts, abandoned and busy counts per provider and model from `provider_calls`, health shows busy-abandoned calls per provider and model over the last hour, and per-turn tool-call telemetry stays unpersisted. pipeline/pipeline/elitellm/client.py server/internal/ops/read_store.go server/internal/ops/types.go ops/src/pages/costs.tsx ops/src/pages/health.tsx
- Developer Epo decided (2026-09-06, after the first review round) the retrieval service and the ingest worker refuse to start under `APP_ENV=production` when `CAPY_MODEL_CONCURRENCY` is unset and the runbook lists it as required; a reserve must be below its total; the ops busy panel and attempts table count attempts abandoned on a provider 429/503/529 answer only, since a gate refusal writes no row; the ingest busy re-pend backs off on the provider's own Retry-After only, the synthesized client hint never feeds the job backoff; and the stream timers bound each provider read individually so a consumer may await between chunks. pipeline/pipeline/config.py pipeline/pipeline/retrieve/service.py pipeline/pipeline/ingest/worker.py pipeline/pipeline/elitellm/client.py pipeline/pipeline/retrieval/models.py server/internal/ops/read_store.go openwiki/deployment-runbook.md
- Developer Epo decided (2026-09-06, second review round) the production startup refusal applies to the ingest worker role only; a request cancelled during admission is undone by a detached done-callback that abandons the never-sent call row and frees its lease without delaying the cancellation; generate and quiz grade carry the busy wait in the body only since Huma sets no header; and the busy re-pend transaction and the Huma envelope lift are covered by a Postgres-backed test and a client test. pipeline/pipeline/ingest/worker.py pipeline/pipeline/retrieval/accounting.py pipeline/tests/test_store_sql.py src/api/client.test.ts openwiki/observability-metering.md
- Developer Epo decided (2026-09-06, fourth review round) figure caption calls retry any failure, not only busy answers, under the ingest policy of four attempts inside two minutes, because one dropped caption is one figure permanently missing from the index. pipeline/pipeline/retrieval/models.py
- Developer Epo decided (2026-09-06) the workspace owner funds automatic source reparse/reindex batches combining collaborators' edits; exhausted ingest credits pause automatic refresh while durable editing saves continue.
