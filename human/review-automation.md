- Developer Epo decided agent-driven review work (Codex Security source scan, Strix UAT scan, `$review-repository`) runs locally with evidence kept in the ignored `review-results/`, reporting through commit statuses on the exact SHA with a one-line summary as evidence for the releaser only; deterministic checks run in GitHub Actions. scripts/review/report-status.sh scripts/review/common.sh
- Developer Epo decided Codex Security is the source security scanner (Standard scan by default, Deep only on explicit request for a release review) and Strix is the dynamic UAT scanner only, its source mode retired as duplicate coverage. scripts/review/codex-security-scan.sh scripts/review/strix-scan.sh .agents/skills/review-repository/SKILL.md
- Developer Epo decided `deploy-uat.yml` is manual dispatch only because UAT staging and its checks are heavy; code review and agent scans are never a promotion gate; promotion to production hard-fails only when the deterministic UAT gate or the editor perf rerun fails on the candidate SHA. .github/workflows/deploy-uat.yml .github/workflows/promote-production.yml .github/workflows/uat-quality.yml scripts/review/validate-review-boundaries.mjs
- Developer Epo decided the planned phase-two UAT gate jobs are a collaboration load test (chaos peers extended with rooms, duration and `/metrics` assertions), a k6 latency smoke on hot gateway read routes, and a golden ingest journey (upload, index, search, chat with citation); ingest capacity stays measured by `bench/parsers` against the production-shaped host because UAT is capped at one slice, slot and worker.
