- Developer Epo decided agent-driven review work (Codex Security source scan, Strix UAT scan, `$review-repository`) runs locally and deterministic checks run in GitHub Actions; local runs report through commit statuses on the exact SHA so `promote-production.yml` can gate on them. `scripts/review/report-status.sh`, `scripts/review/require-statuses.sh`, `.github/workflows/promote-production.yml`
- Developer Epo decided Codex Security is the source security scanner (Standard scan by default, Deep only on explicit request for a release review) and Strix is the dynamic UAT scanner only; Strix source mode is retired because it duplicates Codex Security coverage. `scripts/review/codex-security-scan.sh`, `scripts/review/strix-scan.sh`, `.agents/skills/review-repository/SKILL.md`
- Developer Epo decided promotion to production hard-fails unless the deterministic UAT gate and editor perf rerun green on the candidate SHA and the `source/codex-security` and `uat/strix` commit statuses are `success` on that SHA. `.github/workflows/promote-production.yml`
- Developer Epo decided local scanner evidence stays in ignored `review-results/` with a one-line summary in the commit status description; no evidence is pushed to GitHub.
- Developer Epo decided phase two UAT gate jobs are a collaboration load test (chaos peers extended with rooms, duration, and `/metrics` assertions), a k6 latency smoke on hot gateway read routes, and a golden ingest journey (upload, index, search, chat with citation); ingest capacity stays measured by `bench/parsers` against the production-shaped host because UAT is capped at one slice, slot, and worker.
